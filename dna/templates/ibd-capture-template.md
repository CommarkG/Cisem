# IBD Capture Template (PLACEHOLDER)
**tags:** [template, ibd, capture, placeholder, raw-parking, gate-exempt]
**Status:** PLACEHOLDER
**Aligned to:** CLAUDE.md §2.6 (IBD protocol) + `dna/ibd/README.md` (the governing home) — explicitly NOT ARCH-00008/ARCH-00230's full node checklist, since IBD is a NAMED, sanctioned gate-exemption (capture-first, D2)

## Sections to populate
- `IBD-NNNN` — the pre-node parking ID (allocated via naming-registry.yaml's IBD counter; OUTSIDE the CISEM-{TYPE}-{SEQ} node system)
- Captured date
- Proposed tag — a SINGLE proposed tag/topic label (not the full VOC-00001 governance tag set — this is pre-governance)
- `status: PARKED-RAW` — the only status an IBD entry carries until promotion
- The raw body — captured VERBATIM, never edited/restructured/summarized/"optimized" (the entire point of IBD — premature structuring buries native vocabulary, D2)
- Source note — who/what produced this raw content (Governor dialogue, external input, a session's essence-harvest, etc.)
- Promotion pointer (added LATER, at promotion time only) — which governed node ID this IBD entry became, once it runs the full §3 gates

## Notes
An IBD capture is NOT duplicated from a "core" IBD instance the way other templates are (ARCH-00408's core-template principle does not apply to raw capture — each IBD entry is inherently unique content); this file exists only to standardize the wrapper metadata (ID/date/tag/status) around genuinely fresh verbatim material.
