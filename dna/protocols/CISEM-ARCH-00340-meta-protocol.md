# Meta-Protocol — how to create any CISEM protocol
**Node ID:** CISEM-ARCH-00340
**Type:** ARCH | **Status:** LIVE — ratified by Governor **foundational decree 2026-07-18** (blanket
approval of all pending, this session). The plan-gate is now LIVE; the PROVISIONAL-ACTIVE leash is removed.
**root_authority:** candidate — a root creation tool; added to ARCH-00011 §7 set on decree.
**Version:** v0.1-provisional | **depth_level:** L2 | **Position in schema:** T-SYS, meta-governance
**Wiring state:** DECLARED (drafted) → CONNECTED (when the meta-wizard implements it) → LIVE (on ratify).
**Goal:** Define how ANY protocol is created — so protocols stop being ad-hoc — AND bootstrap the
plan-gate: this + the meta-wizard are the ROOT that every protocol/wizard/plan flows through.
**Governing corespine:** CS-CREATION-001

## Pocket Declaration
- **reasoning_scope:** the universal protocol-creation contract (bootstrap root)
- **inherited_constraints:** A1–A7, Doctrine, I10 (SSOT), the plan-gate design (BP-0007)
- **output_contract:** required structure + ratification path + SSOT rule + wizard pairing
- **ai_cannot:** self-ratify; leave this PROVISIONAL-ACTIVE forever (audit-enforced)
- **recorded_at:** 2026-07-18

## Why (the bootstrap escape)
A protocol cannot be created "by a protocol" without infinite regress. This meta-protocol is the
ROOT: Opus drafts, Governor foundational-decrees. It + the meta-wizard bootstrap the plan-gate;
after that, everything flows through them.

## 1. Every CISEM protocol MUST contain (checklist)
- [ ] Identity — full ARCH-00008 header (status, position, pocket).
- [ ] Goal (measurable) + governing corespine.
- [ ] The RULE stated as an executable PROCEDURE (not prose).
- [ ] Explicit "does NOT cover" scope boundary.
- [ ] SSOT declaration — this protocol is the ONE home for its concern; mirrors may not diverge (I10).
- [ ] Self-compliance note + Path Rejection Declaration.
- [ ] WIZARD reference — the `/cisem-*` skill that faithfully implements it (wizard MIRRORS protocol).

## 2. Ratification path
`DRAFT → PROVISIONAL-ACTIVE (usable, audit-flagged) → double-ratified (Opus soundness + Governor
authority) → RATIFIED, wiring-state LIVE`. Root protocols (this, the plan-protocol) ratify by foundational decree.

## 3. Wizard pairing (mirror rule)
Every protocol has a wizard that executes it. The protocol is SSOT; the wizard is its runnable form,
VERIFIED to map 1:1 to the protocol's clauses. Ratifying the wizard = "confirmed it mirrors."

## Does NOT cover
Plan creation specifically (the plan-protocol, created THROUGH this). Node field schema (ARCH-00008).

## Self-compliance
Follows its own §1 (identity, goal, executable rule, does-not-cover, SSOT, pocket, wizard-ref). Its
wizard (meta-wizard) is queued for Sonnet (BP-0007).

## Path Rejection Declaration (CLAUDE.md §2.8)
- **Rejected:** leaving this PROVISIONAL-ACTIVE indefinitely — the audit flags it until ratified (guardrail).
- **Rejected:** a wizard that diverges from this protocol (I10 — mirror, not parallel).

## Change log
- 2026-07-18: PROVISIONAL-ACTIVE draft created (Opus, PE pocket) on Governor approval. Bootstrap root; awaiting foundational decree.
