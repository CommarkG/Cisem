# IBD-0013 — Meta-Defaults Cluster (permanency · inheritance · ZF · SSOT · retro-engine · completion · graft) 2026-07-19
**Type:** IBD (vault park) | **Status:** PARKED-RAW — axiom candidates run the 8-Q stress-test; mechanisms need build-prompts/plan. NOTHING here is ratified. Opus CANNOT ratify.
**tags:** [ibd, meta-defaults, permanency, mechanical-by-default]
**source:** Governor (this session) | **trust_tier:** GOVERNOR
**Why parked, not built:** these are 8 large threads dropped while the SPINE (corespine model + first candidate corespine) is in-flight AND gated on the external-feedback round. Per completion (I14) + PARK-BY-DEMAND (§2.7) + Principle 15 (build ONE to flawless before spreading), the disciplined move is to REGISTER all of it with full context + routing, protect the spine, and pick ONE deliberately. "Don't let it sleep" = registered here, each with a route + priority.

## CONSOLIDATION → the core insight (Governor: "consolidate all permanency issues into an axiom")
Items 1,2,3,6,7 below are facets of ONE meta-principle: **CISEM's agreed defaults are mechanically enforced,
inherited, and never skipped — BY DEFAULT, not by reminder.** The AI's native default is prose-that-must-be-
remembered; CISEM's default must be the opposite. Core-first: settle this one axiom and most facets ripple.

## 1. AXIOM CANDIDATE — "Mechanical-by-Default (Permanency)"
When consensus is reached on any rule/gate/prevention, its DEFAULT disposition is **T1 / L1 / L0 hardwired +
mechanically enforced** (Claude Code hooks = CAL, plan-audit checks, git hooks, schema-validation), NOT prose that
the system must be reminded of. "The system runs on different defaults and needs reminding" IS the anti-pattern
(documentation-as-progress). Route: 8-Q stress-test → FOUND-00001. Consolidates: EXISTS≠ACTIVE (I23), hardwired=mechanical (ARCH-00390 CAL), D3/D4.

## 2. AXIOM/PRINCIPLE CANDIDATE — "Inheritance-Evident" (esp. OUTWARD-facing)
CISEM DNA inheritance must be EXPLICIT + evident in EVERYTHING created — and MOST critically in outward-facing
creations (apps / SaaS / solutions for external developers + end users). **Named AI-decoding trigger to hardwire
AGAINST:** "building outward ≠ developing the platform → I'll use my OWN defaults / look outside for patterns +
templates." This trigger is HIGH-probability and corrosive — it would let every external solution silently shed the
DNA. Guard: an inheritance-evidence gate on outward creations (the created artifact must cite/carry its inherited
corespine + doctrine). Route: 8-Q stress-test + a build-prompt for the outward-inheritance gate. Home candidate: CS-CREATION-001 / ARCH-00230.

## 3. ZF-EVERYWHERE (the moat — Governor: "nothing should skip it")
ZF is one of CISEM's most unique moats; NOTHING skips it. Route: an RQC audit that every creation PATH (each wizard,
each tier, each outward build) routes through ZF; close any path that bypasses it. Mostly true already — needs the audit to PROVE it, not assume it.

## 4. ONE-SSOT, ALL-MODELS (Governor: "everything from one source; all elements + all models use it")
Every concept has ONE home (ssot-registry.yaml, A8) AND every ELEMENT references it AND every MODEL/tier bootstraps
from it. Route: RQC coverage audit — (a) every governed concept has an ssot-registry entry; (b) mirrors don't diverge;
(c) every agent file (opus/sonnet/haiku) bootstraps from the SSOT, not from context. Partly exists (ssot-registry + agent bootstrap); needs the coverage proof.

## 5. GRANULAR INTERNAL TAGGING + OVERNIGHT BATCH (Governor's direct question)
Today: file-level tags/status wired (RAW-EXTERNAL, disposition, statuses); INTERNAL-section tagging is NOT wired.
**Opus best-balanced recommendation:** do NOT tag every internal line in real-time (kills flow, heavy). Split by value:
(a) REAL-TIME + mechanical only for HIGH-VALUE governed blocks (status/decision/seed/core blocks — the checks we have);
(b) the EXHAUSTIVE heavy sweep (granular internal tagging of everything) runs as a SCHEDULED BATCH (overnight/weekly,
~2am) by a CHEAP tier (Haiku) or a local LLM — it's mechanical enumeration, Haiku's exact lane. Route: build-prompt for
the batch tagger + a scheduler (see §6; needs CronCreate/OS cron — Claude hooks are event-based, cannot do time-based).

## 6. RQC / RETRO-ENGINE UPGRADE (Governor: "make sure it's active + powerful; it should PARK assignments; overnight completion once/day")
Honest status (verified 2026-07-19): RQC = commit-time WARN linter (plan-audit, 8/24 invariants) + manually-logged
quality-ledger. NOT autonomous, does NOT park assignments, does NOT run overnight. **Upgrade design (park):** (a) RQC
findings become PARKED ASSIGNMENTS in a work-queue (not acted-on per-issue — parked, per Governor); (b) an OVERNIGHT
run (once/day) DRAINS the queue — batch tagging (§5), completion sweep (§7), consolidation audits (§3/§4), learning
harvest. Governability: needs a scheduler (CronCreate or OS cron). Route: a plan (this is blast-bearing + scheduler-dependent).

## 7. COMPLETION-HARDWIRING (Governor: "don't let it sleep; hardwire + mechanically enforce; TOP PRIORITY")
Completion (I14) currently relies on the MODEL resurfacing open threads — exactly what just nearly failed. Make it
MECHANICAL: an open-items / un-closed-thread check (parked decisions past TTL already = P3; extend to: presented items
with no resolution, vaulted conflicts with no review date, PROVISIONAL-ACTIVE nodes not ratified). Essential for the
platform AND for external developers building on it. Route: **HIGHEST priority of this cluster** → a plan + a check.
Note: building this NOW, amid 8 threads, would ITSELF be the shiny-object failure — so it's the first DELIBERATE build after the spine, not a scattered add.

## 8. GRAFT MACHINE — research/feedback escalation (Governor: "best point for external vs internal; 3 circles; best graft machine")
Three circles, staged by cost/latency/trust — **use inner circles first + most, outer sparingly:**
- **Circle 1 — virtual expert personas (internal):** cheapest, fastest, always-available. Use FIRST + MOST — an
  ADVERSARIAL panel (multiple experts from different directions, each trying to REFUTE), on every significant decision.
- **Circle 2 — inner-circle platforms (Governor-controlled apps):** trusted, real DIFFERENT architectures (higher signal
  than personas). Use at MILESTONES for direction-validation (as the 3-platform convergence just did).
- **Circle 3 — external AI systems:** highest latency, needs patience + strict de-brand/verify (import-saturation). Reserve
  for FOUNDATIONAL junctures only — not routine. Feedback is proven super-valuable but the value accretes slowly.
**The graft:** every circle's output enters the SAME governed intake (RAW → PURIFY → verify → archive); it is grafted
onto the design ONLY after purification + verification, NEVER raw. Mechanism candidate: a Workflow (persona panel =
parallel adversarial agents) for Circle 1; the RAW-EXTERNAL pair for Circles 2/3. Route: a design doc/plan for the graft machine.

## ROUTING SUMMARY
- Axioms (1,2) → 8-Q stress-test (with suspicion) → FOUND-00001. | Audits (3,4) → RQC sweep tasks.
- Mechanisms (5,6,7,8) → build-prompts/plan. **Completion-hardwiring (7) = HIGHEST priority.**
- ALL gated behind: finish the SPINE first (corespine model + first candidate corespine + feedback round).
