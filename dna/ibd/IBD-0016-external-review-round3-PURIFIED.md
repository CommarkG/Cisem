# IBD-0016 — External Review Round 3 (PURIFIED via PAE 6-lens) 2026-07-19
**Type:** IBD (analysis layer) | **Status:** PURIFIED · UN-RATIFIED — absorption only, NOT folded/reacted-to. Claims to verify.
**tags:** [ibd, purified, external-review, typed-governance-graph]
**Source (raw):** [[IBD-0016-external-review-round3-RAW]] (reviewer R3, EXTERNAL-UNTRUSTED). Engine: [[CISEM-ARCH-00399-purification-alignment-engine]] (6 lenses).
**De-brand note (Vocab + Security lenses):** stripped house references (XACML/OPA/Temporal/NASA/NIST/SRE named authorities, their exact YAML key-style) BUT preserved the semantic constraint each encodes, renamed to CISEM vocab. Their citations = supporting evidence, not adopted authority.

## REVIEWER R3 — UNDERLYING AXIOMS (Axiom lens — map, do NOT adopt)
- Separate the FOUR problems (authority / classification / adjudication / safe-continuation) — do not encode as one tree.
- Operational architecture = a TYPED GOVERNANCE GRAPH (kernel + domains + overlays + a separate conflict control-plane), not a pure inheritance tree.
- Conflicts are between TYPED CLAIMS (with jurisdiction/rule-class/epoch), never whole-corespine rank.
- GATES before SCORES (a scalar score gets optimized/gamed by the AI).
- VERSIONED EPOCHS: in-flight work stays pinned to the epoch it began under; migrate explicitly.
- Corrigibility WITHOUT weakening authority (typed inheritance + waivers, not "WHAT never changes").
- Separation of powers = the real independence (role-separation, not model-diversity).
- Conflict DETECTION (coverage) is harder + more important than resolution.

## ESSENCE UNDER MUTUAL-TITLES (T1–T6)
- **T1 Conflict model.** Wrap the 3 layers in two control stages: (A) detect+normalize BEFORE, (B) debt-retire+version+migrate AFTER. Type every conflict as Claim-vs-Claim {jurisdiction, rule_class, epoch}; rule_class taxonomy (constitutional_invariant→…→heuristic→empirical_assumption) determines the combining logic (a prohibition constrains an optimization; two mandatory constraints = an invalid state, not a balanced fight). Precedence = a TYPED SEQUENCE (applicability→rule-class→constitutional→specificity→ratified-waiver→optimize→default→**INDETERMINATE**), NOT a total order (a total order breeds a "super-domain"). Scale = MULTI-DIMENSIONAL gates first (reversibility/blast-radius/consequence/uncertainty/time/precedent/data-exposure/safe-fallback/epoch), scoring second. Vault→"cemetery" unless it has owner+SLO+max-age+expiry-behavior+recurrence+blocked-count+escalation. Recurring conflict ≠ auto-new-rule → classify ROOT CAUSE first.
- **T2 Sealed-boundary + re-grounding.** "Sealed" must be an ENFORCEABLE state {exploratory | ratified_sealed | amendment_open} moved only by a re-grounding protocol. Re-grounding = a new EPOCH + a regrounding_packet (migration_plan, rollback, affected descendants/apps, independent_challenge). In-flight work + deployed apps stay PINNED to their epoch unless migrated (avoids the "refactoring storm" / inheritance-invalidation). Breaks first at: dual-parenting, cross-cutting-forced-into-hierarchy, descendant needing an exception, a top-level with two jurisdictions, two corespines producing identical constraints, or un-migratable descendants.
- **T3 Significance.** Reach necessary NOT sufficient (reach is gameable / path-dependent — schema places descendants, placement makes reach, reach validates schema = circular). Admission = 4 GATES first (independent jurisdiction · irreducibility · stable invariant core · legitimate constraint authority), THEN evidence (reach/criticality/stability/testability), THEN negative evidence (overlap/dual-parenting/procedural/impl-specific). Counterfactual test: what breaks if removed/demoted; does it survive as top-level across 3 structurally-different instances.
- **T4 Development method.** "Until flawless" → REJECT the absolute. "Build one before generalizing" = keep. Replace with: build → falsify aggressively → 3 STRUCTURALLY-DISSIMILAR instances (constructive · dissimilar · adversarial-that-should-be-REJECTED) → promote on explicit thresholds. "Flawless" → **"fit for governed promotion"**: zero unresolved findings WITHIN a declared test-envelope + documented residual risks + mechanical invariant tests + adversarial case + rollback + ≥2 dissimilar instances + a rejected-candidate test.
- **T5 Self-governance & blind spots.** (a) Legislator-and-advocate = EVALUATOR CAPTURE (even with Governor final say) → separate roles mechanically (proposer/interpreter/detector/challenger/verifier/summarizer/executor/auditor); model-diversity ≠ independence if shared corpus/framing/ontology/reward. (b) Purification = SEMANTIC LAUNDERING risk → require a purification_trace ledger (removed branding, term substitutions, semantic_compressions, OMITTED material, uncertainty_preserved, dissent_preserved); RAW stays the evidentiary authority. (c) "Inherited WHAT never changes" too absolute → typed inheritance {mandatory | mandatory_unless_waived | default | objective | advisory} + a child may INITIATE inapplicability/waiver/exception/amendment/re-grounding (else "dishonest compliance"). (d) Weak conflict DETECTION → every decision carries a constraint_coverage statement; distinguish "no conflict found" from "none within the retrieved set." (e) Governor = single semantic point of failure → ratification_packet = decision COMPRESSION (exact ask, strongest support/opposition, consequences of approve/reject/defer, dissent), and a DIFFERENT AI writes the opposition case. (f) Archive can POISON precedent → disposition MUST appear in every search result (an old rejected option retrieved without its disposition reads as support).
- **T6 Governance mechanics / wizard.** Wizard starts with "is a new corespine JUSTIFIED?" not "create." 0-existing-home (demonstrated, not asserted) → 1-type-classify (most requests are NOT top-level: kernel/top/subordinate/overlay/policy/procedure/template/pattern/knowledge-asset/not-governance) → 2-invariant+jurisdiction → 3-overlap+inheritance → 4-conflict-simulation (incl. malicious/unavailable-Governor/deployed-app) → 5-SANDBOX (no production/propagation/precedent) → 6-diverse-instance falsification → 7-disposition → 8-Governor ratify → 9-versioned activation+monitoring.

## CONVERGENCE / DIVERGENCE vs R1 (Claude) + R2 (Gemini)
- **Triple-convergence (R1+R2+R3):** triage/scale must be mechanical-not-inferred (R2 det.-invariants ≈ R3 gates-first ≈ R1 triage-regress); vault needs drain-discipline+expiry (all three); provisional must not propagate/ossify (R1 C3 ≈ R2 ≈ R3 "inheritance contamination"); significance reach-insufficient + add impact/gates (R1 C5 ≈ R2 impact-criticality ≈ R3 4-gates); build-to-flawless overfits → diverse instances (R1 C6 ≈ R2 triangulation ≈ R3 3-dissimilar); echo-chamber/independence (R1 C8 ≈ R2 compliant-alignment ≈ R3 evaluator-capture); purification semantic-drift (R2 raises ≈ R3 semantic-laundering + purification_trace). → Three INDEPENDENT axiom-sets converging = the strongest signal we have that these are real, not stylistic.
- **R3-distinctive (new beyond R1/R2):** the kernel+domains+OVERLAYS typing (cross-cutting ≠ hierarchy); TYPED CLAIMS + rule_class combining logic; precedence as a typed SEQUENCE ending in INDETERMINATE (not a total order); GOVERNANCE EPOCHS + migration; typed INHERITANCE with child-initiated waiver/exception; constraint-COVERAGE / detection-before-resolution; ROLE-SEPARATION (separation of powers); the purification_trace ledger; disposition-in-every-search-result.
- **Divergence to flag for consolidation:** R3 REJECTS the "constant = temporarily valid" AXIOM (wants "binding-within-epoch + controlled amendment" instead) — this DIRECTLY conflicts with the Governor's axiom candidate (IBD-0012 AX-CAND-A). A real fork for the consolidation step; do NOT resolve now.

## STATUS: ABSORBED — not folded. Consolidation of all reviews (R1+R2+R3+any more) under T1–T6 happens later, all-at-once.
