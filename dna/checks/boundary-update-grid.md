# Boundary Update Grid — what MUST stay current at every compact / tab-switch
**Why (Governor 2026-07-20):** continuous development with NO ~30% loss when we compact or move tabs. State lives on DISK (git-SSOT), never in a model's memory. This grid is the standing checklist the boundary-prep (Principle 12/13) verifies + updates — referenced by the Definition-of-Done (dna/checks/definition-of-done.md) and COMPACT STATE.

## FOUNDING PRINCIPLE — every NODE is a CONTEXT CARRIER (Governor 2026-07-20)
Each node carries its own context — goal, status, tag, governing corespine, parent, links, why-it-exists — so nothing is lost when it is read cold in a fresh tab (A1 nothing-stands-alone + A3 schema-place + Cross-Boundary Zero-Context). A node without its context is an orphan. **Axiom-candidate:** "a node is a context carrier; it must be readable cold, with zero conversation dependency." Route: 8-Q stress-test → A-series (relates to A1/A3 + the context-envelope, ARCH-00392). NOT-YET-an-axiom (candidate).

## THE GRID — the lists that must be current + coherent at every boundary
| List | SSOT home | Verify at boundary |
|---|---|---|
| **Axioms** A1–A9 | FOUND-00001 (+ §2.4 mirror) | new/changed axioms wired + mirrored; guardrails corrected |
| **Schema** (placement / naming / numbering) | SCHEMA-00001 + naming-registry | every created element has a schema place + a registered number; alignment gate (BP-0011) |
| **Skills** | .claude/skills/ (create/plan/meta) | each references the persona SSOT; HTML page current (BP-0011) |
| **Agents** | .claude/agents/ (haiku/sonnet/opus-pe) | each references the persona SSOT; living profile current (AI-profiling §5); HTML page (BP-0011) |
| **Pipelines** | ARCH-00400 (governed-creation) + PAE ARCH-00399 + plan-audit | status current (SOUND-FOR-TRIAL / debts N3/N4) |
| **Corespines + candidates** | corespine-registry.yaml (incl. CANDIDATES block) | every candidate has status + tag; aligned to schema |
| **Parked / rejected / decisions** | parked-and-rejected-register.md · archive-ledger.yaml · IBD-0017 | all routed, none floating |
| **Root insights** | root-insights.yaml (RIPL) | each found-wrong→improved event extracted |
| **Learning + continuity** | learning-registry + WITNESS COMPACT STATE | harvest run; resume path stated |
| **Governor Insights** | dna/governor-insights.yaml | every decree indexed to its home + grouped; new insights land here first (reference-not-copy) |

## DO-AT-EVERY-BOUNDARY
1. Harvest (STEP 1–5) → learning-registry. 2. Update COMPACT STATE (+ the grid above). 3. Every list current + coherent (schema-aligned, status+tag on each). 4. Commit + PUSH (0 unpushed; DoD item 6). 5. State resume path.
**Mechanical enforcement = a BUILD (BP-0009 propagation-network + BP-0011 alignment gate); this grid is the SSOT it wires to — currently behavioral (NOT-YET-WIRED).**
