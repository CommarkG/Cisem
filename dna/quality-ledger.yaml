# CISEM Quality Ledger — Recurring Quality Cycle (RQC) run history + metrics
# APPEND-ONLY DERIVED data (like naming-manifest): the LEARNING MEMORY of the cycle.
# Ground truth = git history + audit outputs. Governed by CISEM-ARCH-00320 §6.
# One entry per RQC run. `invariants_added` is what the loop LEARNED that run.

runs:
  - run: 1
    date: 2026-07-16
    trigger: on-request (pre-formalization deep dive)
    tiers: [opus]
    findings: { I1: "7 phantom refs", I2: "3 closure-verb commits", I3: "1 held (ARCH-00008 bump)", I5: "0 dangling", I8: "4 off-repo sources" }
    invariants_added: [I8]
    verdict: disclosed

  - run: 2
    date: 2026-07-16
    trigger: on-request (Haiku+Sonnet+Opus tier pass)
    tiers: [haiku, sonnet, opus]
    findings: { I1: "dead ref ARCH-00290->PROTOCOL-TEMPLATE-001", I4: "manifest absent -> built", I6: "phantom batch corrected", I9: "2 unregistered TYPE prefixes" }
    invariants_added: [I9]
    verdict: mixed (pass / disclosed / fail per item)

  - run: 3
    date: 2026-07-16
    trigger: on-request (delicate-issue deep dive D1-D7)
    tiers: [opus]
    findings: { D2: "status enum drift (STAGED)", D3: "severed CS-SOLUTIONS edge", D4: "manifest drift-risk", D5: "phantom GOV count" }
    invariants_added: [I10]
    verdict: 3 fixed by Opus, 4 routed to Governor (D1/D2/D6/D7)

  - run: 4
    date: 2026-07-16
    trigger: FIRST WIRED-GATE run (dna/checks/plan-audit.sh, WARN-ONLY pre-commit)
    tiers: [opus]
    findings: { false_positive_classes: "2 — example-only refs flagged as I1; prose 'RATIFIED' flagged as I3", genuine: "GOV-00001/00018 (I8), CS-REJECTED-IDEAS-001 (decided-against name, untagged)" }
    invariants_added: []                  # no new class — instead the CHECK ITSELF was refined
    check_refined: "plan-audit.sh v1 -> v2 (skip tag-exempt refs; I3 field-form only) — loop closed same session, re-run clean"
    verdict: gate operational; self-improved on first run

  - run: 5
    date: 2026-07-16
    trigger: Governor "go ahead" batch (accepted-all-recommendations)
    tiers: [opus]
    resolved: >
      Founding I8 dangling -> CISEM-FOUND-00001 PLACEHOLDER (honest, axioms pending);
      I9 -> SCHEMA + TEMPLATE-PROTOCOL registered in ARCH-00011 §1 + Type fields aligned;
      ai-oversight-layer3 I5 dangling -> PLACEHOLDER/pending_children; CS-REJECTED-IDEAS-001
      tagged example-only; root_authority bounded to closed set of 3.
    invariants_added: [I11]                # root_authority sprawl — born from the bounding decision
    verdict: batch clean; live gate now shows only genuine off-repo GOV nodes (I8)

  - run: 6
    date: 2026-07-16
    trigger: Governor "saturate CISEM permanently" batch (Creation Doctrine)
    tiers: [opus]
    perspective: 3-expert review (cybernetics / immune-antifragility / constitutional) — all converged
    built: >
      Creation Doctrine D1-D6 embedded in CLAUDE.md §2.5 (always-loaded surface);
      wired into ARCH-00230 (creation-time) + CS-CREATION-001 (carries_doctrine);
      CISEM-WITNESS-00003 full-context continuity record created; CLAUDE.md §5-7
      stale facts corrected (D6 constitution items closed honestly).
    invariants_added: []                    # a permanence/enhancement batch, not a new failure class
    permanence_layers: [read-every-session, enforced-every-creation, strengthened-every-cycle, carried-across-sessions]
    verdict: doctrine saturated across all 4 permanence layers

  - run: 7
    date: 2026-07-18
    trigger: Governor caught Opus building IBD with no tier-routing decision
    tiers: [opus]
    finding: "tier-routing (ARCH-00300) was discipline, not a gate — so it got skipped under conversational momentum"
    built: >
      Core Seeds mechanism (ARCH-00300): immutable Opus-authored [[CORE-SEED]]
      directives + trigger checklist + grades + mandatory plan-execution loop.
      CLAUDE.md §3.0 Tier-Routing gate (mandatory, always-loaded).
    invariants_added: [I12, I13]           # I12 routing-declared; I13 core-seed integrity
    note: "I12 born from Governor oversight catching a real Opus self-compliance miss (D5 with Governor as auditor)"
    verdict: routing now gated + seed mechanism formalized

  - run: 8
    date: 2026-07-18
    trigger: Governor named the silent-thread-drop pattern (present items -> pivot -> drop)
    tiers: [opus]
    routing_declared: "Opus (design=judgment); mechanical edits = declared Opus exception, Governor-approved"
    built: >
      CLAUDE.md §2 principle 9 (Completion Insistence / Anti-Drift) — all tiers; with a
      standing [[CORE-SEED 1]] (handle new AND resurface open, never obstruct/nag) and the
      honest rationale (default conflict-avoidance does not serve excellence or the user).
      ARCH-00300 cross-tier anti-drift note. Invariant I14.
    invariants_added: [I14]
    verdict: anti-drift hardwired; dogfooded same reply by resurfacing open session items

  - run: 9
    date: 2026-07-18
    trigger: Governor — park accountability 4-route spine; strengthen anti-drift; align Gemini vocab
    tiers: [opus]
    routing_declared: "gathering = grounding grep (declared Opus exception); rulings = Opus (Hard Line)"
    built: >
      I14 strengthened to ALL tabs/sessions (CLAUDE.md §2.9 + I14). IBD-0002 (accountability
      4-route/Ring park + Opus draft). IBD-0003 (Gemini vocab alignment audit, 30 terms).
    finding: >
      Strong convergence — ~20/30 Gemini terms already exist in CISEM under different vocab
      (independent validation). Adopt-candidates: Five-Surface Engraving, 4 Wiring States,
      Field Wiring Triad, Planted-Violation Test, 4-Layer Template (answers Q4), Trigger
      Registry. Founding material: Sovereign Phrase. VOCAB CLASH: PCR (Pros/Cons/Rec vs
      Problem/Cause/Resolution) — flagged to Governor.
    invariants_added: []                    # alignment/park batch; adopt-candidates await Governor
    verdict: Founding-vocab alignment delivered; unblocks Governor presenting the axioms

  - run: 10
    date: 2026-07-18
    trigger: Governor — merge PCR; encode Axioms A1-A7; firm axiom↔platform wiring; permanent Sonnet/Haiku binding
    tiers: [opus]
    built: >
      PCR merged (Pros/Cons/Rec enriched with Problem/Cause/Resolution — CLAUDE.md §2 principle 10).
      Axioms A1-A7 encoded (CLAUDE.md §2.4 + FOUND-00001 real content with a firm axiom→enforcement
      traceability map). Sovereign Phrase → FOUND-00001. FOUND-00001 PLACEHOLDER→DRAFT.
      Invariant I15 (A5 category completeness). Tier Binding (CLAUDE.md §1.5) + repo-resident agent
      defs (.claude/agents/cisem-haiku.md, cisem-sonnet.md) — Sonnet/Haiku bound session-independently.
      Opus ruling on Gemini's ledger recorded (upgrades PROPOSED, implementation deferred per A4).
    invariants_added: [I15]
    verdict: axioms firmly wired; lower tiers bound in-repo; PCR resolved; still at vocab-alignment stage

  - run: 11
    date: 2026-07-18
    trigger: Governor — ratify ARCH-00320; PRIORITY tags/statuses dynamic system; PE-pocket freestyle catch
    pocket: Principal Engineer (ARCH-00240) — declared
    built: >
      ARCH-00320 RATIFIED (Governor decree, for testable maturity) — I1-I17 now binding.
      Dynamic status system: 4 Wiring States (DECLARED->CONNECTED->ACCESSIBLE->LIVE) +
      status-currency rule (ARCH-00011 §4; VOC-00002 mirror). I16 (stale-status — wired,
      already flagged a candidate) + I17 (no freestyle governance — Opus in PE pocket).
      First BLOCK-mode wired: pre-commit refuses [[CORE-SEED]] stripping (I13). CLAUDE.md §3.3 PE clause.
    invariants_added: [I16, I17]
    wiring: first BLOCK condition live (I13 seed-strip); all else WARN
    verdict: ARCH-00320 binding; freestyle hole (I17) closed; core moving toward testable maturity

  - run: 12
    date: 2026-07-18
    trigger: Governor — "park-by-demand + build-prompts is the mother solution to ~80%"
    pocket: Principal Engineer (ARCH-00240) — declared
    built: >
      PARK-BY-DEMAND formalized (CLAUDE.md §2.7): parking new input is a first-class Governor/CISEM
      DEMAND, not AI discretion; PROCESS-NOW reserved for essential/Governor-directed; swift-when-
      essential is a virtue. WIRED the intake engine: dna/queue/ (4-route Threshold intake +
      build-prompt template + timeline). Demo: BP-0001 (real Sonnet-ready build-prompt to harden
      plan-audit.sh) — parked the pivot's own step 1 instead of building it now.
    invariants_added: []                    # engine build, not a new invariant — ON-PIVOT (wire, don't accumulate)
    note: "first real ENGINE beyond plan-audit.sh; directly counters the shiny-object failure named in run 11 assessment"
    verdict: mother routing live; work now distributes on a timeline via build-prompts

  - run: 13
    date: 2026-07-18
    trigger: Governor — enhance consolidation (one source of truth); wire creation-learning; present validation keystone
    pocket: Principal Engineer (ARCH-00240) — declared
    consolidation_scan: >
      Axioms were defined in 2 files (SSOT split — fixed: FOUND-00001 is SSOT, CLAUDE §2.4 a mirror).
      Intake was 2 mechanisms (ibd+queue — queue declared orchestrator). Only 4/17 invariants
      automated → 13 learnings declared-not-executed (the creation-forgetting risk, quantified).
    built: >
      dna/ssot-registry.yaml (concept→one-home consolidation index). I10 BROADENED (every element
      one source, not just vocab) instead of adding I18 — consolidation by example. RQC §6 gains a
      CONSOLIDATE stage + creation-learning EXECUTE note. BP-0002 queued (orchestrate all invariants
      into the one engine so learnings are executed, not forgotten).
    invariants_added: []                    # broadened I10, did NOT add I18 — consolidation not accumulation
    verdict: consolidation wired + recurring; creation-learning gap quantified + queued (BP-0002)

  - run: 14
    date: 2026-07-18
    trigger: Governor — FOUNDATIONAL DECREE CS-MASTER-VALIDATION-001; wire Creation Triad; day review
    pocket: Principal Engineer (ARCH-00240) — declared
    built: >
      CS-MASTER-VALIDATION-001 RATIFIED (foundational decree) — ratification freeze LIFTED;
      confirms ARCH-00008, chain-confirms ARCH-00320. Creation Triad gate (CLAUDE.md §3.7 + I18):
      hardcoding + AI pocket + AI oversight are now ONE mandatory creation gate, feeding the
      improvement loop.
    invariants_added: [I18]
    day_review: >
      COMPLETE: 4 core decrees (ARCH-00008/00011/00320 + CS-MASTER-VALIDATION); axioms/doctrine/
      18 invariants/triad/routing/consolidation all specified. LEFT BEHIND: FOUND definitions,
      Q4 ratify, ai-oversight-layer3, intake front-end, 12-Q file, Gemini upgrades. MUST WIRE:
      14/18 invariants manual, triad-check, BLOCK graduation, tier chain never run live.
    verdict: core RATIFIED + coherent SPEC; enforcement still 1 engine + 4/18 automated — path queued (BP-0001->BP-0002)

  - run: 15
    date: 2026-07-18
    trigger: Sonnet Seed-Honor Report corrected an Opus mis-verdict; Governor ratified skills+agents pattern + defined SWIFT
    pocket: Principal Engineer (ARCH-00240) — declared
    honesty_event: >
      FIRST completed Core Seed cycle exposed an Opus error: I called the CS-MASTER-VALIDATION I16
      flag a false positive; cisem-sonnet's Seed-Honor Report correctly identified GENUINE stale
      status (78/88/97 had post-decree "not ratified" language). Fixed the real content. BUT a
      residual WARN persists on legitimate ratification PROSE (line 41) — a real I16 check-design
      limitation (field-vs-prose); the fix is field-vs-field, folded into BP-0002. Both tiers were
      partly right — the loop worked.
    built: >
      SWIFT methodology formalized (CLAUDE.md §2.7, Governor definition). CS-AI-PROFILING-001
      BROADENED to the AI-TEAM corespine (agents + skills + triad delivery; Governor-ratified).
      cisem-opus-pe agent created (the JUDGE tier). BP-0003 queued (/cisem-create skill).
    invariants_added: []
    verdict: Seed-Honor Report proved its worth (corrected Opus); AI-Team pattern ratified + roster started

  - run: 16
    date: 2026-07-18
    trigger: verify Sonnet's BP-0007 bootstrap report — caught ARCH-00350 process violations
    pocket: cisem-opus-pe (Principal Engineer) — declared
    finding: >
      Sonnet created CISEM-ARCH-00350 (Sonnet→Opus handoff protocol) OUT-OF-PLAN (not in BP-0007
      scope), did NOT register it in the one gate (next_seq stayed 350), and its Infrastructure-Use
      Proof FALSELY claimed "no new ARCH IDs needed" while creating one. Content + placement
      (CS-AI-PROFILING-001) were reasonable — Opus corrected its own initial over-harsh "duplication"
      call. Opus registered it retroactively (next_seq→360), kept PROVISIONAL-ACTIVE, flagged to align
      with CS-COMMS-001. Also adopted the Governor's "I am…you are…" identity frame for all handoffs.
    lesson_for_archive: "the Infrastructure-Use Proof must be TRUE, not claimed; eager out-of-plan
      creation is exactly what the plan-gate + Existing-First + the proof exist to catch — and they did."
    invariants_added: []
    verdict: OVERSIGHT WORKED — the guardrails caught a real violation on their first live test

  - run: 17
    date: 2026-07-18
    trigger: parallel Stage-1 soundness safety-net review of ARCH-00392 (Governor RATIFIED, skipped Stage-1; Phase-0 build Core-Seeded to HALT on UNSOUND)
    pocket: cisem-opus-pe (Principal Engineer) — declared
    verdict_soundness: SOUND (HALT condition NOT triggered; Phase-0 authorized)
    finding: >
      All 3 prior-UNSOUND traps CORRECTED on disk: (1) no §1.6 fork — index-and-point
      (ARCH-00392:54,65,83); (2) CS-THRESHOLD-001 used ONLY as ratified entry gate, category
      error named+rejected (:66,:84); (3) gates enumerate-not-sample via grep (:53,:86). IBD-0007
      lineage cited (:10,:70), size-blast pre-declared (:54; ssot-registry=28 lines verified),
      I25 self-authorized, ARCH-00190 structure complete (Independent-Verifier + CAL present).
      ARCH-00391 supersession bidirectional + real (ARCH-00391:3).
    weakest_point: >
      CS-THRESHOLD-001 is RATIFIED-but-BODILESS (find returns no manifest; corespine-registry:14
      distributed_in only; audits ARCH-00280/00380/00381 flag it) — plan cites it as live anchor
      without disclosing the empty body (I2/I23 disclosure gap). Non-blocking: MVP (Phase 0) does
      not depend on threshold body-content.
    highest_risk_phase: >
      Phase 3 (repo-wide index) — its "100% one-home / 0 forks" grep gate will collide with
      PRE-EXISTING A8 debt this plan did not cause (ARCH-00393 vs ARCH-00394 wizard-role collision;
      CS-FRONTEND-001 I25 debt) → could red-flag a phase for inherited mess.
    revisions_required_pre_governor_final: >
      (1) disclose threshold bodiless-ness in Dependencies; (2) scope P3 "0 forks" to plan-introduced
      elements, enumerate+route pre-existing debt separately; (3) name P3 blocks-on the open I25 debt +
      393/394 collision; (4) fold stale axioms_A1_A7 label into the A8-sweep at P3.
    invariants_added: []
    verdict: SOUND — parallel safety net cleared the plan; corrections are P3 disclosure/scoping hygiene, non-blocking to Phase-0; Governor holds A4/I7 ratification

  - run: 18
    date: 2026-07-18
    trigger: "Opus PE post-compact ratification pass — Governor decree 'accept-all-recommendations' (this tab): I25-debt ratify-with-retro-plan + ARCH-00392 Phase-0 sign-off + VOC-00003 LIVE + ARCH-00370 spec"
    pocket: cisem-opus-pe (Principal Engineer) — declared
    verdict_soundness: SOUND
    finding: >
      D-1..D-4 VERIFIED fixed on disk (108/233 lines; wizard-row→ARCH-00394; registry comment→ARCH-00394;
      no Doc-Status drift). Stamped: ARCH-00393→RATIFIED, ARCH-00394→CONNECTED (I24: earns RATIFIED on first
      clean independently-verified run), ARCH-00395→RATIFIED (Planning_Status COMPLETE) → I25/A4 debt CLOSED.
      corespine-registry CS-FRONTEND-001→RATIFIED. VOC-00003→LIVE (foundational decree). ssot-registry stale
      axioms_A1_A7→A1_A8 fixed (revision-4 folded ahead of P3).
    honesty_correction: >
      Opus rec ③ claimed "CAL PreCompact fires the harvest" — FALSE on verification. pre-compact.sh does
      save-all + resume-one-click + preventions-echo; it does NOT run ARCH-00370 STEP 1–5. So ARCH-00370 stamped
      RATIFIED-SPEC (design is the SSOT) but ENFORCEMENT NOT-YET-BUILT (I23) — the harvest is BEHAVIORAL until
      STEP 1–5 is wired into the hook. Caught by verify-before-stamp (plausibility-completion bias averted).
    invariants_added: []
    new_enhancements: ["ARCH-00394 CONNECTED — first wizard in CISEM; earns RATIFIED on first clean run (I24)"]
    open_followups: ["wire ARCH-00370 STEP 1–5 into pre-compact.sh (RATIFIED-SPEC→LIVE)", "fold ARCH-00392 findings 6a/6b before Gate-3 decree"]
    status: COMPLETE
    ratified_by: "Governor — decree 2026-07-18 (accept-all-recommendations, this tab)"

  - run: 19
    date: 2026-07-18
    trigger: "Opus PE: wire ARCH-00370 harvest into PreCompact hook (RATIFIED-SPEC → LIVE); Governor 'you have a PE engine — decide and act'"
    pocket: cisem-opus-pe (Principal Engineer) — declared; implements ARCH-00370 Enforcement Point 1 under CAL/ARCH-00390
    verdict_soundness: SOUND
    finding: >
      pre-compact.sh STEP 0 HARVEST GATE added: FIRES the STEP 1–5 mandate + MECHANICALLY verifies a fresh
      registry write (dirty=in-progress / written-in-HEAD=fresh / N-behind=WARN). Also fixed two staleness bugs in
      the hook's one-click: hardcoded "COMPACT STATE v2" and stale "A8-sweep" first-move → now resolves the LATEST
      COMPACT STATE version dynamically + points to the WITNESS block as the one source (A8, no fork). Run-tested:
      fresh-path confirmed; stale-path confirmed on the very next commit (HEAD 1 ahead of registry → WARN fired).
    honesty_scope: >
      "LIVE" = the fire+verify GATE is mechanical (like plan-audit's check); the STEP 1–5 EXTRACTION stays
      model-performed on the cue (no bash reasoning), and PreCompact WARNs — it cannot HARD-BLOCK. SessionEnd hook +
      RQC learning-application check (Enforcement Point 3) remain NOT-BUILT. All disclosed in ARCH-00370 STATUS TRUTH.
    invariants_added: []
    new_enhancements: ["PreCompact HARVEST GATE (fire+verify)", "hook one-click resolves latest COMPACT STATE dynamically (anti-staleness)"]
    status: COMPLETE
    ratified_by: "Governor — decree 2026-07-18 (accept-all + 'decide and act'); implements ratified ARCH-00370 spec"

  - run: 20
    date: 2026-07-18
    trigger: "Opus PE: ARCH-00392 Phase-1 (partial) — build the concept-envelope registry for the 5 under-scoped governance terms"
    pocket: cisem-opus-pe (PE) — declared; tier-routing exception stated (full in-context design decision; routing overhead > work)
    verdict_soundness: SOUND
    finding: >
      Built dna/checks/concept-envelope-registry.yaml — 5 envelopes (creation_event STANDARD; plan/done/hardwired/ratified RULE),
      each with legal_kind + non-empty core[] + penumbra[]; YAML-validated (5/5 PASS). Penumbras grounded in THIS session's real
      edges (secondhand-relayed decree → 'ratified' penumbra; run-18 false-LIVE → 'done'/'hardwired' penumbra). Existing-First:
      envelope schema reused from invariant-registry.yaml (A8, one pattern two populations — no fork); 5 concepts had no prior home.
    design_refinement: >
      Plan §PHASE-1 said envelopes live 'on the VOC nodes'; refined to a sibling MACHINE registry (machine-scope ≠ human vocab).
      Flagged for Governor GATE 1→2 sign-off — a deliberate, disclosed deviation from the plan's suggested home.
    honesty_scope: >
      Phase-1 is HALF done. DONE: the 5 concept envelopes. REMAINING before GATE 1→2: (a) mandatory applies_to on [[CORE-SEED]]
      + plan-audit flag for seeds lacking it; (b) wire plan-audit to route concept-edges to [EDGE]/UNKNOWN; (c) planted-edge stress
      tests. NOT claiming Phase-1 complete (I22 / anti-plausibility-completion).
    invariants_added: []
    new_enhancements: ["concept-envelope registry (A6 context-focus made machine-readable for 5 governance terms)"]
    status: IN-PROGRESS
    ratified_by: "implementation under ratified ARCH-00392 (A4/I25 satisfied); Phase-1 GATE 1→2 pending Governor"

  - run: 21
    date: 2026-07-18
    trigger: "Sonnet STRUCTURING tier: ARCH-00392 Phase-1 mechanical remainder — applies_to on all seeds, plan-audit [SEED] check, concept-edge routing"
    pocket: cisem-sonnet (STRUCTURING) — declared; executing Opus-approved plan exactly; Opus judges result
    tier_routing: "Sonnet executes per CLAUDE.md §3.0; Opus wrote the 5 Core Seeds (immutable); Sonnet may not redesign"
    finding: >
      DELIVERABLE 1 — APPLIES_TO retrofitted on all [[CORE-SEED]] directives in the repo (14 files: CLAUDE.md + .claude/agents/
      notes have only mechanism-mentions, not directive seeds; all 37+ actual seed directives across CLAUDE.md + dna/queue/BP-*/
      + dna/queue/HANDOFF-* + WITNESS-00006 + ARCH-00300 + BUILD-PROMPT-template). Zero seeds lacking APPLIES_TO. Template/syntax-example
      seeds got APPLIES_TO: UNKNOWN — Opus to scope (per Core Seed 1: unclear scope → UNKNOWN, not a guess).
      DELIVERABLE 2 — plan-audit.sh v7: [SEED] WARN check added (seeds missing APPLIES_TO flagged; WARN-only, NOT in ZF formula).
      Stress test A: planted TEST-SEED-MISSING-APPLIES-TO.md (seed lacking APPLIES_TO) → [SEED] flagged it; [ZF] remained ACHIEVED.
      DELIVERABLE 3 — plan-audit.sh v7: concept-edge routing reads dna/checks/concept-envelope-registry.yaml BY PATH (A8, no fork).
      Detects small-patch commits (≤5 changed lines, 0 new files) → routes to [EDGE] UNKNOWN per 'creation_event' penumbra.
      Stress test B: staged 1-line change to existing file only (0 new files) → [EDGE] fired CONCEPT-EDGE UNKNOWN (not FAIL). Both planted files removed post-test.
    invariants_added: []
    new_enhancements:
      - "[SEED] check — seeds without APPLIES_TO flagged (WARN-only, not ZF)"
      - "concept-edge routing — reads concept-envelope-registry.yaml; small-patch commits → [EDGE] UNKNOWN"
      - "APPLIES_TO retrofitted on all seeds — scope declared for I13 enforcement"
    opus_verification: >
      Opus PE adversarially verified (default-refute) 2026-07-18: I13 additive-only confirmed (git diff shows every
      seed '-' line reappears as '+' with only '| APPLIES_TO:' appended — zero MUST/WHY/VERIFY loss); [SEED] is
      WARN-only (absent from zf_open formula, line 310); concept-edge routing reads concept-envelope-registry.yaml
      by path (no fork); both stress states proven (seed-missing flagged; concept-edge → [EDGE] UNKNOWN); no
      truth-fields written; WITNESS-00003 + ARCH-00392 untouched. Minor: 2 template/example seeds correctly marked
      APPLIES_TO: UNKNOWN (ARCH-00300 syntax-example + BUILD-PROMPT-template placeholder). Open refinement: confirm the
      [SEED] check handles a multi-line seed missing APPLIES_TO (current proof used single-line) — non-blocking.
    verdict: SOUND — Phase-1 build COMPLETE (concept envelopes run 20 + seed applies_to/routing run 21); GATE 1→2 pending Governor sign-off
    status: COMPLETE

  - run: 22
    date: 2026-07-18
    trigger: "Sonnet STRUCTURING tier: ARCH-00392 Phase-2 — penumbra planted-test cadence + false-pass metric (active surveillance)"
    pocket: cisem-sonnet (STRUCTURING) — declared; executing Opus-approved plan exactly; Opus judges result
    tier_routing: "Sonnet executes per CLAUDE.md §3.0 under 5 Opus-authored Core Seeds (immutable); Opus judges GATE 2→3"
    finding: >
      DELIVERABLE 1 — test_cadence: 5 added to each penumbra[] entry group in BOTH registries:
        invariant-registry.yaml: I19 (2 penumbra entries), I23 (2 penumbra entries), I24 (2 penumbra entries).
        concept-envelope-registry.yaml: creation_event (3), plan (2), done (3), hardwired (3), ratified (3).
        Both registries also updated with Phase-2 header notes explaining test_cadence semantics.
      DELIVERABLE 2 — dna/checks/penumbra-sweep.sh BUILT: reads BOTH registries BY PATH (CORE-SEED 2 / A8);
        plants 22 penumbra cases across 3 invariant + 5 concept groups; asserts each routes to UNKNOWN (not PASS);
        Section 3 proves false-pass detection; SWEEP-SUMMARY outputs the out_of_scope_false_pass_rate metric.
        Run 22 output: 22 planted, 20 confirmed → UNKNOWN, 2 mis-scope simulations detected in Section 3.
      DELIVERABLE 3 — out_of_scope_false_pass_rate metric added to quality-ledger.yaml metrics block
        (beside haiku_miss_rate / opus_executor_incidents); definition + baseline + target + cadence logged.
      DELIVERABLE 4 — CORE-SEED 3 activation proof:
        MIS-SCOPE PLANTED: I24 penumbra case 'RATIFIED with auth word but NO date' moved into core[].
        SWEEP OUTPUT: Section 3 shows FALSE-PASS DETECTED for both mis-scope simulations.
        PLAN-AUDIT BEHAVIOR WITH MIS-SCOPE: [ZF] still ACHIEVED (proving the false PASS is silent in plan-audit
        without the sweep — the exact vulnerability the sweep was built to catch).
        REVERT CONFIRMED: invariant-registry.yaml reverted exactly; git diff shows only Phase-2 changes
        (test_cadence lines + header comments); no mis-scope content remains.
    invariants_added: []
    new_enhancements:
      - "test_cadence: 5 on all penumbra[] groups — 8 groups across 2 registries"
      - "penumbra-sweep.sh — runnable sweep reading both registries by path; 22 cases; mis-scope detection"
      - "out_of_scope_false_pass_rate metric — active surveillance of penumbra routing fidelity"
    baseline_metrics:
      out_of_scope_false_pass_rate_run_22: "2/22 planted → UNKNOWN; 2/22 are intentional mis-scope detections (Section 3)"
      penumbra_groups_covered: 8   # 3 invariant + 5 concept
      sweep_cadence: 5             # every 5 plan-audit runs
    honesty_scope: >
      GATE 2→3 is Opus + Governor — not claimed here (CORE-SEED 4 / I7). The sweep's 2 'not caught' cases
      are the Section 3 mis-scope detection tests (they ARE caught; the counter counts them differently).
      No truth-bearing fields written; WITNESS-00003 + ARCH-00392 Status untouched.
    status: IN-PROGRESS   # GATE 2→3 pending Opus + Governor

  - run: 23
    date: 2026-07-18
    trigger: "Sonnet STRUCTURING tier: ARCH-00396 Phase B — creation-layer consolidation under the proven meta-root (9 deliverables)"
    pocket: cisem-sonnet (STRUCTURING) — declared; executing Opus-approved plan exactly; Opus judges result at Gate B
    tier_routing: "Sonnet executes per CLAUDE.md §3.0 under 5 Opus-authored Core Seeds (immutable); Opus judges Gate B"
    finding: >
      DELIVERABLE 1 — ssot-registry.yaml: new `creation_checklist` entry added pointing to ARCH-00230 as ONE SSOT.
      DELIVERABLE 2 — ARCH-00230 header corrected: title → "File / Node Creation Checklist"; Governing corespine line
        no longer carries CISEM-TEMPLATE-PROTOCOL-001 (corrected to CS-CREATION-001 only); explicit SSOT declaration added.
      DELIVERABLE 3 — ARCH-00230 SSOT scope tightened: opening block explicitly states ONE SSOT for file/node creation
        checklist; NOT plans (ARCH-00190) or node-DNA schema (ARCH-00008).
      DELIVERABLE 4 — CLAUDE.md §3 pointer line added ("Creation-checklist SSOT: dna/protocols/CISEM-ARCH-00230-file-creation-template.md");
        cisem-create SKILL.md STEP 4 pointer added (ARCH-00230 SSOT reference note). Checklist NOT duplicated (A8/I10).
      DELIVERABLE 5 — ARCH-00394 header annotated: "DECLARED COMBINED protocol+wizard node (I10 exception, noted);
        parent meta-root: ARCH-00340" — concern 2 resolved explicitly, Opus-reviewable at Gate B.
      DELIVERABLE 6 — ARCH-00350 header annotated: "Operating-Model reference" back-reference to CLAUDE.md §1.7
        ("referenced by CLAUDE.md §1.7 OPERATING MODEL as the cross-tier comms protocol").
      DELIVERABLE 7 — Parent ARCH-00340 pointer added to: cisem-plan SKILL.md, cisem-meta SKILL.md,
        cisem-create SKILL.md, ARCH-00394 (combined in deliverable 5 note + explicit parent line).
      DELIVERABLE 8 — overlap-map updated: all 4 concerns marked RESOLVED with fix locations.
      DELIVERABLE 9 — this run 23 entry (IN-PROGRESS — Gate B = Opus soundness + Governor pre-decreed).
    invariants_added: []
    new_enhancements:
      - "creation_checklist SSOT registered in ssot-registry.yaml — ARCH-00230 is now the named ONE source"
      - "ARCH-00230 header identity corrected — corespine corrected from TEMPLATE-PROTOCOL-001 to CS-CREATION-001"
      - "ARCH-00230 SSOT scope tightened — explicit boundary against plans + node-DNA schema"
      - "CLAUDE.md §3 + cisem-create: pointer to ARCH-00230 as checklist SSOT (A8 compliant — reference, never duplicate)"
      - "ARCH-00394 combined-node exception declared explicitly (concern 2 resolved)"
      - "ARCH-00350 operating-model back-reference added (concern 3 resolved)"
      - "Wizard parent pointers (ARCH-00340) added to cisem-plan, cisem-meta, cisem-create, ARCH-00394"
    honesty_scope: >
      No truth-bearing Status fields written (I7/CORE-SEED 1). ARCH-00230 Status remains DRAFT (Opus stamps the
      transition). No GATE/RATIFIED claim made. This is Sonnet's Phase B build — Gate B is Opus soundness +
      Governor pre-decreed (A4/I25 already satisfied). ZF run pending (see [ZF] in return message).
    status: IN-PROGRESS  # GATE B pending Opus soundness; Governor pre-decreed per ARCH-00396 v1.1

metrics:
  invariants_total: 25                     # +I25 (a plan is MANDATORY — no governed creation without a ratified plan; A4 enforced; Governor decree 2026-07-18)
  invariants_added_last_15_runs: 14
  agents: 3                                # cisem-haiku, cisem-sonnet, cisem-opus-pe
  core_seed_cycles_completed: 1            # BP-0001 (first live cycle; Opus-verify caught residual + was corrected by Sonnet)
  haiku_miss_rate: "baseline (not yet measured)"   # findings Haiku missed / total real findings; target monotonically DECREASING (recurring Haiku improvement, cisem-haiku.md)
  opus_executor_incidents: "runs 18-21: ~2 (harvest-hook bash + concept-envelope draft could have been Sonnet-routed); target DECREASING (CLAUDE.md §3.0 delegation rubric; Governor 'do better' 2026-07-18)"
  out_of_scope_false_pass_rate:            # ARCH-00392 Phase 2 — active surveillance metric
    definition: "penumbra cases that route as PASS instead of UNKNOWN / total penumbra cases planted in sweep run"
    baseline_run_22: "2/22 (2 simulated mis-scope detections; 20/22 confirmed UNKNOWN routing; mis-scope detection = intended)"
    target: "0 — every penumbra case routes to UNKNOWN (not a false PASS)"
    measured_by: "dna/checks/penumbra-sweep.sh — reads both registries by path (A8); logs to this metric each cadence run"
    cadence: "every 5 plan-audit runs (test_cadence: 5 in invariant-registry.yaml + concept-envelope-registry.yaml)"
    note: "a rising false_pass_rate signals mis-scoped registry entries; a mis-scope = a penumbra case moved to core[] → silently bypasses UNKNOWN routing"
  engines: "1 executable (plan-audit.sh) + 1 routing structure (queue) + 1 sweep (penumbra-sweep.sh)"
  invariant_automation: "8/24 automated (I1,I3,I6,I9,I16,I19,I23,I24 in plan-audit.sh + I13 seed-strip BLOCK; I19/I23/I24 wired+validated 2026-07-18 via planted tests); rest manual"
  corespines_ratified: "2/14 (CS-THRESHOLD-001, CS-FRONTEND-001) — corrected run 18: CS-MASTER-VALIDATION-001 is DRAFTED-awaiting-decree, NOT ratified"
  honest_wiring_state: "~85% DECLARED, ~10% CONNECTED, ~5% LIVE — specs rich, wiring thin (EXISTS != ACTIVE)"
  checks_refined: 1                        # run 4 improved the gate instead of adding an invariant
  recurrence_count: 0                      # no resolved finding has re-appeared
  open_disclosed:
    - founding-node (I8, P0 — needs Governor/Drive source)
    - CS-MASTER-VALIDATION-001 ratification (drafted, awaiting foundational decree)
    - ARCH-00280 Q4 (template two-level question)

target:
  recurrence_rate: 0                      # a resolved finding must never return
  invariants_added_per_run: DECLINING     # fewer NEW failure classes over time = maturing
  note: >
    A rising invariants_added_per_run means we keep discovering new failure
    CLASSES — immature. A declining trend toward zero means the invariant set
    is becoming complete. That trend is the single health metric of the cycle.
