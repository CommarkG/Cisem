# dna/knowledge-library/ — uploaded files + external research findings (indexed for AI recall)
**Governed by:** CLAUDE.md §3.2b (Existing-First). **Enforced by:** `plan-audit.sh [I19]` (a plan/creation
must state its Existing-First search, which INCLUDES this library).

## Why
The Governor requires a knowledge library so research (e.g. the DIOS constitution) is not lost or re-derived.
Two hard requirements: (1) every item **tagged + status'd**; (2) every item carries **total tags + internal-part
tags + a summary**, held in `index.yaml`, so any AI (Haiku/Sonnet/Opus) can find relevant knowledge fast.

## Hardwired consultation (all tiers, all situations)
On ANY topic change, and before ANY plan or creation, every tier MUST consult `index.yaml` as part of
Existing-First — not from memory. Off-repo sources (Drive) are cited by fileId (I8) and read on demand.

## Status uses the DIOS maturity scale
Signal → Observation → Hypothesis → Pattern → Insight → Principle-Candidate → Validated-Principle → Constitutional.

## Scheduled
A deeper wiring + full-series ingestion pass is scheduled (Governor 2026-07-18).
