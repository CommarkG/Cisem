# Session Close / Resume Anchor — 2026-08-18
**Type:** HARVEST / RESUME-ANCHOR | **Status:** REFERENCE-ACTIVE | **tags:** [harvest, resume, session-close, arch-00435, operating-model]
**RESUME PATH:** CLAUDE.md → this file → `dna/learning-registry/root-insights.yaml` → `dna/planning/CISEM-ARCH-00435-*.md` (disposition pending) → the open Governor decisions below.
git HEAD at close = **e6f8b7f** (ARCH-00435 v1.0 committed+pushed).

**I2C ACTIVE — permanent, all tiers (CLAUDE.md §1.8 decree 2026-08-18):** terse colleague-style; NO summaries / (A)(B) packets / boundary recaps / tier-narration; converge to consensus; nothing decided until Governor EXPLICITLY ratifies. KEEP: honesty (no unverified "done"), pushback, no build/truth-stamp without ratify.

## THE FRUITS (latest decisions — full fidelity, A10)

### 1. ARCH-00435 — GI-68 found it is a DUPLICATE; Opus recommends WITHDRAW (AWAITING GOVERNOR DISPOSITION)
- Drafted this session (evidence-discipline plan-audit checks: `[EVIDENCE-CLAIM]` + `[DUAL-REVIEW]`); committed **e6f8b7f**, AWAITING-BUILDER-REVIEW.
- **GI-68 dual-review (verified at source):** BOTH steps are functional duplicates of checks in the very file they target —
  - `[EVIDENCE-CLAIM]` ≈ **`[DOD]` item (b)** (plan-audit.sh:536): same enforce-word list + diff-scoped + evidence-gate + WARN.
  - `[DUAL-REVIEW]` ≈ **`[RATIFY-GATE]`** (plan-audit.sh:668), already extended (ARCH-00420-PART01, itself GI-68-reviewed) to require a `## GI-68 Dual Review` citation, with a shipped fixture harness. The stricter "haiku+sonnet substring" match STEP 2 proposed is a **deliberately-REJECTED RI-0012 pattern** (comment at ~plan-audit.sh:690).
- **Opus Stage-1 "SOUND" verdict WITHDRAWN — it was wrong** (Existing-First matched bracket-names, not function).
- **ARCH-00433 FINDING 4 was ALREADY CLOSED** by `[RATIFY-GATE]` — do not rebuild.
- **PENDING Governor disposition:** withdraw ARCH-00435 (Opus rec: class already enforced by `[DOD]`(b)+`[RATIFY-GATE]`) vs re-scope to a tiny `[DOD]`(b) whole-file→adjacent-line tightening (marginal; 74% of dna/**.md already use the vocabulary). If withdrawn: archive-ledger entry + record FINDING 4 already-closed.

### 2. RI CANDIDATE (route to root-insights) — Existing-First-by-function, not by-name
A new plan-audit check's Existing-First MUST read the **bodies** of the 2-3 most-recent sibling checks + match `dna/checks/fixtures/` **by purpose**, not grep the bracket-name. **Functional-duplication-under-a-different-name** is a distinct failure mode from literal-name duplication. (Would have caught the ARCH-00435 miss. Sonnet-harvested, Opus-confirmed.)

### 3. Brain corrections (accepted)
- **Item 53:** the portable "Governing an AI-Built Codebase" doc is **CISEM's OWN restatement**, NOT external — restatement is NOT corroboration. Opus's earlier "independent re-derivation validates CISEM" framing is WITHDRAWN.
- **Item 55:** a ratification token **pasted into a prompt** is a declaration the agent can echo, **NOT a control**. Only a value the agent cannot read is a gate. (Corrects any turn-contract design — ratification must be held outside the agent's reach.)

### 4. Operating-model — UNRESOLVED disposition
`CDS-MULTI-MODEL-WORKING-GUIDE.md` (ratified) = **Opus-main tab + delegate to Sonnet/Haiku**. The wired setting is `~/.claude/settings.json → model: sonnet` (**Sonnet-main**) — the OPPOSITE. Governor rejected "hybrid" (distrusts Opus judgment on cost after repeated over-spend). **Decision open:** align to CDS Opus-main, or keep Sonnet-main. Enforcement-layer plan (turn-contract etc.) needs A11 Brain consult; Brain pre-corrected items 53/55.

### 5. Marketing-visuals scatter (ecosystem, not this repo)
CSP `marketing-and-sales-engine` (canonical: materials/taxonomy/catalog/classification/image-bank) + Trial Marketing visuals app (`studio_spec.json` ratified + branding-techniques + stale ref-copies) + **crystal-normalizer** (built this session — genai + normalize + local-studio) overlap heavily; crystal-normalizer's `local_studio.py` duplicates Trial's `studio_composite.py`. `~/.claude/ecosystem-index.md` is STALE (2026-05-16). Consolidation plan not yet drafted.

### 6. crystal-normalizer (SEPARATE non-CISEM repo)
`C:\Users\finky\Desktop\Claude Code\crystal-normalizer` — built + working (Gemini 3 Pro / GPT Image 2 / free Local Studio; `keys.env`; Flask trial bench with compare + downloads). NOT a CISEM artifact; not in this repo.

## THE META-PATTERN (worth engraving)
Three times this session Opus committed the exact class under discussion: (a) item-53 restatement-as-corroboration; (b) "the commit didn't land" — a conclusion drawn from a mid-flight query; (c) ARCH-00435 duplicate-as-new. **This is the strongest argument that mechanical gates matter more than any model's self-discipline — Opus included.** The GI-68 loop caught (c); Brain caught (a); a settled query caught (b).

## OPEN GOVERNOR DECISIONS (carried, not dropped)
1. **ARCH-00435 disposition** — withdraw (Opus rec) vs re-scope.
2. **Operating-model** — Opus-main (CDS guide) vs Sonnet-main (wired).
3. **Marketing-visuals consolidation** — draft a plan? (CSP=home; Trial=studio_spec; crystal-normalizer=genai).
4. **UX/UI verdict relayed 2026-08-18:** the OmniMedia "Enterprise Architecture Dashboard" does NOT deserve "Template hub page" as-is (self-certification chrome + internal jargon + FE-I13 contrast fails on Step-2/search). Fix = lead with templates/user-value, demote metrics, translate jargon, fix contrast.
