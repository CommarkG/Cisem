# Boundary Verbatim + Interpretation Index (2026-07-21) — READ BOTH after compaction
**tags:** [boundary-survival, verbatim, interpretation, read-both, anti-summarization, resume]
**Status:** ACTIVE (post-compaction resume anchor — the AI MUST read BOTH the verbatim source AND the interpretations)
**Governor decree 2026-07-21:** "I want real verbatim + your interpretations and I want AI after compacting to read BOTH." Reconstructing verbatim from memory = summarizing again (the sin). So the VERBATIM source of truth is the raw transcript; the interpretations are the CISEM files.

## (A) VERBATIM SOURCE OF TRUTH — the raw session transcript (perfect fidelity, zero summarization)
`C:\Users\finky\.claude\projects\c--Users-finky-Desktop-Claude-Code-Cisem\c249d411-23b2-46c5-9695-ddbaf884b7de.jsonl`
- This .jsonl holds EVERY user (Governor) message and assistant turn VERBATIM. To recover the Governor's exact asks after compaction, EXTRACT the `"role":"user"` message texts from it (grep/jq), do NOT rely on any summary.
- Rule: when in doubt about what the Governor MEANT or SAID, the transcript is authoritative over any COMPACT STATE / fidelity summary.

## (B) INTERPRETATION LAYER — the CISEM files (Opus's understanding, design, decisions)
- [[session-fidelity-2026-07-21-creation-process]] — Governor asks + Opus understanding + good/bad remarks.
- [[creation-process-merge-design-2026-07-21]] — the merged creation-process design + full reasoning/nuances.
- [[creation-external-input-default-and-pcr-2026-07-21]] — external-input default + ID-collision PCR.
- [[IBD-0027-creation-core-spine-brain-PURIFIED]] — the absorbed Brain draft.
- Archive DEC-0011..0014 — the rejected alternatives with reasoning.

## RESUME INSTRUCTION (for the post-compaction AI)
1. Read (A) the transcript (extract the Governor's verbatim user messages) AND (B) all interpretation files above. BOTH, not one.
2. Verify your understanding against the VERBATIM (transcript wins over any summary — Step 0).
3. Consensus state: Step 0 · depth levels D1/D2/D3 · orchestrator-gap · merge-shape (mandatory-steps→forks) · Validating-as-sibling · corespine-determination-once-at-Fork-D — all agreed.
4. Pending: arrange full merge details · confirm CS-CREATION-001=content-merge · does the orchestrator own tier-routing · then draft→TEST-RUN(walk)→consolidate→formalize. NO code until "formalize".

## HONEST LIMIT
The pre-compaction portion of THIS session is NOT in the current context verbatim (only the prior COMPACT STATE summary captured it) — that verbatim is only recoverable from the transcript (A). Going forward the pre-compact hook mandates this index so no future boundary loses verbatim again.
