# QUEUED for NEXT SESSION — "minitree plan" as the default intent-carrier shape
**Status:** PARKED / QUEUED (goal stage — not yet approach or plan). **Provenance:** Governor via Brain, 2026-07-24.
**Do NOT process this session — first thing next session, alongside the fresh GI-68 dual review.**

## The goal (as stated)
Establish **"minitree plan"** in the vocabulary as the DEFAULT intent-carrier shape — **merge ARCH-00190
(plan protocol) and ARCH-00330 (mini-tree split)** so plans are **minitree-structured FROM CREATION**, not split
reactively after they bloat past the §3.6 size gate.

## Why it matters (connects to this session)
Every substantive plan this session (ARCH-00417/00418/00419/00270) hit the §3.6 200-line ceiling and took a
documented exception — the exact structural tension named in **RI-0026**. Making plans minitree-shaped by default
dissolves that: no reactive split, no exception, the shape is right from birth.

## Scope discipline (explicit)
- Its own **small, properly-scoped RENAMING plan (per GI-15 consistent-naming)** — NOT a blanket find-replace.
- Runs through the standing process: **goal → approach → plan → ratify → build.** This file is the GOAL capture only.
- Reuse-before-coin: "minitree" must be registered in the vocabulary with its industry equivalent (GI-15), and the
  merge must honor A8 (one home — ARCH-00190 and ARCH-00330 consolidate, not fork).

## The other two items already queued for next session (pointers, already on disk)
1. **GI-68 dual review of ARCH-00270 v2** (graduated BLOCK-flip, tightening folded, Opus Stage-1 SOUND) → then Governor Stage-2.
2. **Stage-timer plan (ARCH-00420, not yet drafted)** — PAUSED behind `freshness-check.sh` (confirmed not built).
   Sequenced after/alongside that shared primitive.

## Resume state
git HEAD **d37707a** at pause · working tree clean · `[ZF] ACHIEVED`. Nothing half-built; all in-flight plans are
committed drafts with Opus Stage-1 verdicts recorded.
