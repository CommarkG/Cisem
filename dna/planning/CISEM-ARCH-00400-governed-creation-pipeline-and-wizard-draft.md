# Governed-Creation Pipeline + Corespine Wizard — DRAFT v2
**Node ID:** CISEM-ARCH-00400 | **Type:** ARCH | **Status:** PROVISIONAL-ACTIVE-FOR-TRIAL (Mod3 — direction ratified; SEALS only after the first-corespine trial green-gates, never before)
**Planning Status:** AWAITING-INDEPENDENT-RE-REVIEW (v1 verdict was UNSOUND; v2 routes all findings)
**Goal:** One pipeline for how CISEM governs creation + resolves conflict, folding the 6-reviewer conclusions, so the first corespine can be TEST-DRIVEN on a sound frame. Measurable: every A1–A7 + §B conclusion has a LOCATED home+gate here (not name-dropped); re-review returns zero unrouted findings.
**Governing corespine:** CS-CREATION-001 (apex) | **depth_level:** L2 | **tags:** pipeline, conflict-resolution, wizard, graph, verify-seam
**Independent Verifier:** cisem-opus-pe (cold, default-refute) — REQUIRED before any seal (this IS conclusion A1). Honest residual (H2): the verifier shares the author's base model, so it REDUCES — does not CLOSE — the judge-jury seam; genuine model-independence comes from the recurring EXTERNAL review cadence (§Part4).
**Ratified basis (Governor 2026-07-19):** conclusions A1–A7 + §B (consolidation-conclusions-2026-07-19.md); Fork C → typed governance GRAPH; Fork D → keep-and-harden the axiom; M2 → validation in the KERNEL + default precedence VALIDATION > CREATION (resolves DEC-0004); Mod3 → seal only post-trial.
**Pocket:** scope=design pipeline+wizard from ratified conclusions · constraints=A1–A8,D1–D6,I1–I25,ARCH-00190/00382/00394/00399 · output=located home+gate per conclusion · ai_cannot=self-ratify, self-clear, declare done without the cold verifier · recorded 2026-07-19 · cal ARCH-00390.
**EXISTING-FIRST (§3.2b, Mod1):** searched ssot-registry, corespine-registry, ARCH-00394 (wizard, RATIFIED), **ARCH-00382 (unified-graph plan, PROVISIONAL-ACTIVE)**, IBD-0012, consolidation-conclusions, archive-ledger (DEC-0001..0006). RELATIONSHIP: ARCH-00400 = the GOVERNANCE graph (kernel/domain/overlay/control-plane structure); ARCH-00382 = the KNOWLEDGE graph (queryable node/edge store of all artifacts). Distinct, complementary; the knowledge graph INDEXES the governance graph. ENHANCES ARCH-00394 (Part 3 relationship stated, M4); does not fork.

## PART 1 — THE GRAPH ARCHITECTURE (Fork C)
Four typed structural kinds:
- **Constitutional KERNEL** (root frame) — authority/ratification · **validation (M2: CS-MASTER-VALIDATION-001 lives here)** · evidence/traceability · versioning/epochs · safety/containment. **Immutability is NOT mechanical (H1, honest):** the AI writes the whole repo, so nothing it authors can stop it editing the kernel. The real seal = **Governor ratification + git review**, optionally hardened by a human-signed out-of-repo anchor; a pre-commit path-guard only RAISES the cost (it is itself AI-editable — residual named). The AI PROPOSES-against the kernel, never silently edits it; a kernel diff without a Governor-signed ratification is a violation surfaced at review.
- **DOMAIN corespines** — governed domains, hierarchical beneath the kernel; cumulative inheritance.
- **Cross-cutting OVERLAYS** — security/privacy/observability/accessibility: apply ACROSS domains, NOT trunks.
- **Conflict CONTROL-PLANE** — a separate plane (Part 2).
- **Schema-placement (Mod2, A2/A3):** the kernel + control-plane ARE the schema ROOT FRAME (the genesis-block placement); they satisfy A2/A3 as the root that everything else attaches to — declared carve-out, not floating.
- **Genesis + cardinality (M1):** the kernel + top-level domain/overlay set is a GENESIS BLOCK ratified once by fiat (reach un-measurable at genesis). Reach later validates/PRUNES **and the STRUCTURAL-WRONGNESS protocol can trigger a re-grounding to ADD** a domain when indirect evidence (system strain / recurring conflict patterns / unresolvable vault items) shows the cardinality was set too low. "Never births" is corrected: genesis births by fiat; structural-wrongness re-grounds to add; reach prunes.

## PART 2 — CONFLICT-RESOLUTION (control-plane; A3/A4 + §B)
Conflicts are between **typed CLAIMS** {jurisdiction, rule_class, epoch}, never whole-corespine rank. rule_class (constitutional_invariant → mandatory → prohibition → optimization → default → heuristic) selects combining logic. **Overlay claims (M3)** are typed too: a mandatory overlay constraint (e.g. privacy) enters the SAME precedence by its rule_class and can constrain a domain optimization; overlays do not need a trunk rank — rule_class ranks them.
- **Detection FIRST:** every decision carries a `constraint_coverage` statement; "no conflict found" vs "none within the retrieved set" are distinct (R3).
- **Layer 1 — default presumption:** typed precedence SEQUENCE — applicability → rule-class → **VALIDATION-over-CREATION (M2 default)** → constitutional → specificity → ratified-waiver → optimize → default → **INDETERMINATE**. NOT a total order (DEC-0006). Sample-audit high-frequency auto-resolutions (R4).
- **Layer 2 — triage by GATES not a score (A4):** mechanical gates (reversibility · blast-radius · consequence · safe-fallback · precedent-risk · data-exposure). **FAIL-UP** when unsure (R4). Sample-audit provisional-proceed verdicts (R5/R6). Outcomes: rigid-default · provisional-proceed+park · vault-and-halt.
- **Provisional (A7):** non-propagating, version-pinned, `expires_at`, rollback_plan; local execution only, NEVER normative inheritance.
- **Vault drain-SLA (A3):** owner + typed disposition + timeout + DEFAULT-ACTION-ON-EXPIRY (auto-safe→park · needs-human→Governor-ping · blocked→HARD-BLOCK next cycle); "open vaulted" = release-blocking metric.
- **Graceful-degrade halt contract (M7):** when vault-and-halt fires, deployed solutions degrade to last-stable-state + a high-severity flag ("internal conflict — we'll get back to you"), scoped to the narrowest module, NEVER forcing a wrong answer.
- **Rollback / versioned default-swap (M5):** defaults are VERSIONED; L3 emits a NEW versioned default swapped ATOMICALLY after ratification, never mutate in place; every sealed/default change carries a forward-applied inverse (rollback) defined BEFORE activation.
- **Meta-conflict resolver (§B):** disagreement about which layer/rule-class applies → default ESCALATE (named).
- **Governor capacity (§B):** cap N proposals/cycle (overflow queued as a governed artifact); batch-ratification lanes for L3 codifications, distinct from feature ratification.
- **Second-tier-reach monitor (M6):** periodically measure second-tier reach; when one approaches top-level significance → trigger a governance review (boundary-gaming guard).

## PART 3 — THE WIZARD (M4: SUPERSEDES ARCH-00394 §STEP1-6)
Relationship (M4/N6/A8/I10): Part 3's refusal-first 0–9 flow SUPERSEDES ARCH-00394 STEP 1–6 but ABSORBS its naming/registry/manifest mechanics into step 9 activation (NOT discarded); on ratification ARCH-00394 is updated to reference this flow + the old STEP1-6 archived SUPERSEDED (a DEC entry). Header "enhances… does not fork" = this absorb-not-discard relationship. Until then this is the provisional flow.
Flow: 0 existing-home (demonstrated) → 1 type-classify (kernel/domain/overlay/subordinate/policy/…/not-governance — most requests are NOT top-level) → 2 invariant+jurisdiction → 3 overlap+inheritance → 4 conflict-simulation → 5 SANDBOX (no production/propagation/precedent) → 6 diverse-instance falsification → 7 disposition → 8 Governor ratify → 9 versioned activation+monitoring.
- **Significance (A5):** NOMINATE by IRREDUCIBILITY (can't be derived from a combination of the others) + ORTHOGONALITY (refusal-first on the trunk set) — testable at genesis; CONFIRM by reach at maturity (non-destructive proxy: transitive-inheritors × criticality).
- **Inherited WHAT = a MECHANICAL INVARIANT (testable assertion), not prose (A2, Governor-approved).** Guard the WHAT semantically (mandatory cannot-mean checked vs descendant behavior — §B) → blocks upward WHAT-widening.
- **"Flawless" = GREEN GATE (A6):** fixed acceptance suite → zero findings on a clean re-run, N consecutive passes; pre-declare N(≈3) + a DIVERGENCE rule (1–3 don't converge → pattern doesn't exist yet). Instances adversarially DIVERSE + one moderately complex; criteria domain-agnostic, defined BEFORE instance-1. Match method to reach.

## PART 4 — THE VERIFY SEAM (A1 — first standing gate)
A COLD, SEPARATE instance re-derives EVERY "done" from source artifacts, tasked to find FALSE; self-output is UNTRUSTED until independently reproduced. **Independence is PARTIAL (H2, honest):** same base model = shared blind spots, so this REDUCES not CLOSES the seam. **Mechanical trigger (H2, N5 — MVP-to-build, NOT yet wired into CAL; the v1→v2 dispatch was MANUAL):** a creation/ratification event WILL dispatch a cold verifier agent — BLOCKING for HALT-class claims (seal/ratify/kernel-touch), advisory otherwise; the verdict is recorded on the artifact. **Model-independence (H2):** paired with the recurring EXTERNAL constitutional review (§Part5) using DIFFERENT base models — that is the only real cross-model check. Our plan-audit is author-written and does NOT satisfy A1.

## PART 5 — AXIOM (Fork D) + ANCHOR + HYGIENE
- **Axiom (hardened):** "A ratified invariant is binding within its governance EPOCH; challengeable ONLY via a mechanical reopen-artifact (claim-it's-wrong + evidence + named harm) + an INDEPENDENT verifier of 'better' + Governor ratification + a migration plan." **Honesty caveat (M8):** the artifact STRUCTURES the human judgment; it does not MECHANIZE it — the seal's real strength is the Governor's ratification discipline.
- **Constitutional anchor (H1):** the kernel is a human-ratified genesis block the AI proposes-against but cannot legitimately edit; enforcement = Governor + git review (+ optional out-of-repo signed anchor), NOT an AI-authored guard.
- **Institutionalized external review (Min1/N7):** the monthly external-review cadence + graft machine (IBD-0013 #8) are PARTLY-ACTIVE toward R6's periodic mandatory constitutional review; completes when the cadence is fully wired (a trial-phase task).
- **Purification hardening (§B):** PAE (ARCH-00399) gains a claim-level preservation check (PURIFIED preserves ALL material claims of RAW, and only those) + a purification_trace ledger.
- **Archive hygiene (§B):** disposition in every search result; reopen-conditions EVALUATED on retrieval; periodically cluster near-duplicate rejections.

## WHAT CHANGES / WHAT DOES NOT
CHANGES: model tree→graph; precedence total-order→typed-sequence (VALIDATION>CREATION default); triage score→gates+fail-up; flawless feeling→green-gate; WHAT prose→invariant; adds verify-seam(+trigger), vault-SLA, graceful-degrade, rollback/epochs, capacity limits, second-tier monitor, structural-wrongness.
DOES NOT CHANGE (defend — E): RAW/PURIFIED+trust-tier; disposition archive; refusal-first; the axiom's INTENT; Governor sole ratifier; git-SSOT.

## PHASING (Principle 15; Mod4)
- **MVP:** Part 4 verify-seam + mechanical trigger · Part 3 WHAT-as-invariant FORMAT (a testable assertion — specifiable per-corespine independent of the graph; its INHERITANCE propagation waits for Part 1) · Part 2 vault drain-SLA + graceful-degrade. Highest-leverage, smallest surface, gate everything's trustworthiness.
- Then: the graph model (Part 1) via the first corespine TRIAL (Inheritance candidate, IBD-0017 P2) under the green-gate → seal only if it green-gates.
- Then: control-plane precedence + capacity + axiom/anchor wiring.

## PATH REJECTION (§2 principle 8)
Pure tree (DEC-0003 class); total-order precedence (DEC-0006); flawless-as-feeling; prose WHAT (DEC-0005); "AI-mechanically-can't-edit-kernel" (H1 — rejected as false).

## REVIEW STATUS
- v1 → cisem-opus-pe → UNSOUND (2 HALT + 8 MAJOR, 2026-07-19).
- v2 → cisem-opus-pe re-review → UNSOUND-narrow: **14/15 routed**; blocked only by 2 truth-field defects — N1 (SSOT said UN-RATIFIED vs draft "ratified") + N2 (DEC-0004 prematurely closed).
- v3 (this) fixes N1 (consolidation-conclusions marked Governor-ratified-as-basis) + N2 (DEC-0004 → disposition_history + stays VAULTED until seal) + minors N5 (trigger = MVP-to-build, manual so far) / N6 (supersede ABSORBS ARCH-00394 mechanics) / N7 (external review PARTLY-ACTIVE).
**TRIAL-PHASE DEBTS (logged, out-of-MVP, not trial-blocking — fix before the relevant wiring):**
- N3: Part-2 precedence sequence double-counts the rule_class ladder (constitutional/optimize/default appear in both) → make the sequence orthogonal to the ladder (non-class tie-breakers only). Fix before control-plane build.
- N4: CS-CREATION-001 apex vs the KERNEL placement is undefined (two-root recurs one level up) → resolve before any kernel wiring.
NEXT: confirm N1/N2 fixed with the cold verifier → then TEST-DRIVE as PROVISIONAL-ACTIVE-FOR-TRIAL (not seal). NOT sealed until the first-corespine trial green-gates.
