# Harmonious Flow — the Work-Spine View + Seam Mechanization
**Node ID:** CISEM-ARCH-00432 | **Type:** ARCH | **Status:** PROVISIONAL-ACTIVE | **Planning Status:** GI-68-DUAL-REVIEWED — findings folded (v0.3, 2026-08-08); AWAITING GOVERNOR RATIFICATION of the SPLIT scope (see `## Ratification Scope`: D1+D2 build-authorize · D3 sequence-only · D4 own-Stage-1-first · D5 assess-only) — NOT self-ratified (GI-63)
**depth_level:** L2 | **Position in schema:** T-SYS, process-orchestration layer
**tags:** [planning, harmonious-flow, work-spine, mechanization, orchestration, antigravity-informed]
**wisdom_considered:** [the 3-Haiku operating-spine gather 2026-08-07 (14-home A8 scatter finding), ARCH-00419 (pipeline-view — PARENT, consolidated not forked), ARCH-00426 (WEE — the harvest mechanization), ARCH-00406 (RIPL/vault), A8 one-source, I23 EXISTS≠ACTIVE, Principle 22, A12 (Antigravity = external, ideas-adapted-not-copied), the Real-Value/wiring-state schema SCHEMA-00002]
**goal:** unify CISEM's five processes (planning · threshold routing · vault/parking · accountability · harvesting) into ONE visible, mostly-mechanical flow — measurable as: a single GENERATED work-spine board places every open item on the flow (intake→park→plan→build→harvest) with stage/priority/owner/next, replacing the ~11 scattered pending-work homes (enumerated + filesystem-verified 2026-08-08) as the single VIEW (not a 12th list).
**governing corespine:** CS-CREATION-001

## Pocket Declaration (§3.3)
- **reasoning_scope:** orchestrate the harmonious-flow improvements; consolidate existing ratified-unbuilt plans + add the work-spine view + CLI, informed (not dictated) by Antigravity's parallel design
- **inherited_constraints:** A4/A11 · A8 (consolidate, don't fork ARCH-00419/426/406) · A12 (Antigravity is external — adapt ideas, never copy files/claims, never adopt its bypass-permission pattern) · Principle 22 · I10
- **output_contract:** a sequenced build-roadmap whose CORE is the generated work-spine view; each seam-fix cites its parent plan
- **ai_cannot:** create a 15th manual pending-work list; copy Antigravity's Python modules wholesale (native-primitives first); adopt bypass-permission/global-write; mark any deliverable done without a running mechanism (I23)
- **recorded_at:** 2026-08-07
- **Independent Verifier:** cisem-haiku (protocol) + cisem-sonnet (implementability) — GI-68

## Tier-Routing Declaration
Opus (this plan + soundness + the spine-view design) · Sonnet (build the generated view + CLI + light hooks per Opus checklist) · Haiku (enumerate the 14 homes' tags for the view). No new agent.

## Executive Summary
CISEM's five processes are already ONE goal-rooted loop that closes correctly; the harmony gap is (a) pending work smeared across ~14 homes with no single view (A8), and (b) the seam-closers already exist as ratified-but-unbuilt plans (the 85%-declared/5%-live metric, here). Antigravity — a parallel platform the Governor is building — independently mechanized the same flow (a state-lifecycle engine, an active-process router, a transcript-harvester, a unified CLI), proving those seams CAN be mechanical. This plan makes CISEM's flow visible + self-firing by adapting those proven mechanizations to CISEM's native-primitives, with the **generated work-spine view** as the core-first move everything else becomes visible through.

## Anti-Scatter Ordering (Principle 16)
Central, in order: **D1 the work-spine GENERATED view** (makes the whole flow visible — the ripple that aligns everything) → **D2 the `cisem-status` CLI** (its runnable surface) → **D3 mechanize the harvest** (build WEE) → **D4 active-process router** (light SWIFT hook). Peripheral, parked: **D5 BLOCK-mode graduation** (assess only, gated on ARCH-00270 false-positive fixes); the heavier Antigravity mechanisms (hash-lock state machine) — evaluated, not copied.

## The Rule
1. **D1 — WORK-SPINE GENERATED VIEW (core):** a script reading the tags across the **11 distinct pending-work homes** (enumerated + filesystem-verified 2026-08-08): todo-tracker · IBD (incl. high-priority dumps like IBD-0017/0030 — a MEMBER of the IBD home, not a separate home) · BP-queue · queue PARK/NEWTAB · root-insights · session-index · `parked-and-rejected-register.md` · quality-ledger(open) · unbuilt-plans · governor-insights(backlog) · WITNESS → ONE board: `item · stage(intake/park/plan/build/harvest) · priority · owner · next-action · goal-connected?`. **A GENERATED VIEW over existing homes — never a 12th list** (A8). **D1 is NEW construction** informed-by ARCH-00419's 4-home taxonomy (CONSENSUS-REACHED/unratified; its view-script `planning-marker-view.sh` is NOT-YET-BUILT — verified): it WIDENS the *concept* from 4 plan-homes to all 11; there is NO existing code to build upon (I23 — consolidate the idea, not a ghost artifact).
2. **D2 — `cisem-status` CLI/script** (adapts Antigravity's unified CLI): `bash dna/checks/cisem-status.sh` emits D1's board; add `reconcile`/`audit` subcommands later. Native-primitive (bash/existing checks), not a Python module.
3. **D3 — mechanize the harvest STEP 1–5** (adapts Antigravity's transcript-harvester): build WEE (ARCH-00426) L-layers so extraction is hook-fired, not model-performed; the pre-compact hook already fires the mandate + save-raw-activity — add the mechanical extraction.
4. **D4 — active-process router (light)** (adapts Antigravity's threshold hook): a session-start/UserPromptSubmit-class check that flags a parked item matching the ACTIVE plan's tags for swift-integration — a LIGHT surfacing hook, not an auto-unpark state machine (consensus-before-action, GI-03).
5. **D5 — BLOCK-mode graduation assessment** (Antigravity blocks; CISEM is WARN-only): assess which mature checks graduate WARN→BLOCK — gated on the [YAML-VALID] + check-fixture sweeps (a false-positive check must not graduate, ARCH-00270).

## Per-File Alignment Table
| File | ID / registry | Schema placement | tags | status |
|---|---|---|---|---|
| `dna/planning/CISEM-ARCH-00432-...md` (this) | CISEM-ARCH-00432 (naming 432→433) | T-SYS, planning-orchestration | planning, harmonious-flow | PROVISIONAL-ACTIVE / AWAITING-BUILDER-REVIEW |
| `dna/checks/cisem-status.sh` (NEW, D1/D2) | check | T-SYS, check layer | work-spine, view | to-be-created at build (+ fixture, RI-0058) |
| WEE artifacts (D3 — per ARCH-00426 L1–L4) | per ARCH-00426 | T-SYS, CAL/harvest layer | harvest, wee | ARCH-00426 RATIFIED — SEQUENCE the build (no new ratification) |
| `dna/checks/active-process-router.sh` (NEW, D4) | check/hook | T-SYS, check layer | swift, router | NEEDS its OWN Opus Stage-1 before build (has none) |
| ARCH-00419 / 00426 / 00406 | existing plans | — | — | CONSOLIDATED/SEQUENCED by this plan (not forked) |

## What DOES change
One orchestration plan + (at build) one generated work-spine view/CLI; the sequencing of three existing ratified-unbuilt plans; light hooks adapting Antigravity's proven ideas.

## What does NOT change
No fork of ARCH-00419/426/406 (consolidated). No new invariant. No 15th manual list. No adoption of Antigravity's bypass-permission/global-write. No Python-module rewrite of native bash primitives.

## Definition of Done
Per `dna/checks/definition-of-done.md`: the work-spine view RUNS and emits a real board (Principle 18C — shown output, not a doc) + its fixture passes + it reads the actual 14 homes' tags; each consolidated plan (419/426/406) cited with its status; A12 note present. Honest: this plan's VALUE is proven when the board first replaces "where is everything?" with one glance.

## Falsification Tests (per deliverable — discipline ADOPTED from CDS's 7-Tier Harmonious-Flow plan, 2026-08-07)
Each deliverable ships a concrete "do X → assert Y" test that FAILS if the deliverable doesn't behave — verify-behavior baked into the plan (the CDS pattern; the strongest thing about their plan, and CISEM's own Principle-18C operationalized):
- **D1 (work-spine view):** take a KNOWN open item from each of the 11 homes (e.g. a specific IBD-NNNN, a BP-NNNN, an open RI) → the generated board MUST list all of them at their correct stage. FALSIFIES if any known pending item is MISSING from the board.
- **D2 (`cisem-status`):** run it → each count MUST equal the SAME-INSTANT `find`/`ls` count RECOMPUTED at test-run-time — a LIVE relationship (`view-count == filesystem-count`), NEVER a frozen literal (verified 43 IBD / 12 BP on 2026-08-08, but the test asserts the *equality*, so it stays true as the repo grows — Principle 22: no frozen number as an acceptance criterion). FALSIFIES if `cisem-status`'s count ≠ the live filesystem count.
- **D3 (harvest mechanization):** trigger a compact → the hook MUST have WRITTEN the structured extraction to disk (not merely echoed the mandate). FALSIFIES if only the cue fired (the current state).
- **D4 (active-process router):** park an item tagged to match the ACTIVE plan → it MUST be surfaced for swift-integration that session. FALSIFIES if it waits for the weekly batch.
- **D5 (BLOCK-mode):** a check with a KNOWN false-positive MUST NOT graduate to BLOCK. FALSIFIES if a false-positiving check blocks a commit.

## Gap → Deliverable traceability (the seams the operating-spine gather found, mapped)
GAP-1 pending-work scattered across ~14 homes → D1/D2 · GAP-2 queue→plan handoff manual (build-prompt) → D4 + a follow-on · GAP-3 Gate-0/anti-scatter prose-only → cross-ref CDS Tier-4 live-existence-check (a routed-destination-exists guard) · GAP-4 harvest STEP 1–5 model-performed → D3 · GAP-5 no vault/escalate → D5 + WEE build (CDS Tier-6 park↔vault bridge as reference).

## Propagation Declaration
ssot-registry gains a `work_spine` concept → the generated view (SSOT for "what's open + where"). The 14 homes stay their own SSOTs; the view REFERENCES them (I10). The Antigravity exchange doc (dna/external-review/CISEM-Opus__FOR-Antigravity...) records the value-trade + is relayed outward by the Governor.

## Dependencies
blocks-on: Governor ratification (this plan, SPLIT scope). informed-by: ARCH-00419's 4-home taxonomy (CONSENSUS-REACHED/unratified; its view-script is NOT-YET-BUILT — D1 is NEW construction, not built-upon). D3 depends on ARCH-00426 (RATIFIED — sequence-only). unblocks: the single-glance flow view; the harvest mechanization (WEE); the SWIFT router.

## Self-compliance (vs ARCH-00230)
Existing-First: full §3.2b — ssot-registry + naming-registry + the 3-Haiku gather confirmed the 14-home scatter + that ARCH-00419/426/406 already exist (so CONSOLIDATE); Antigravity is external (A12 — ideas adapted). Tags+Status + Pocket + Verifier + Core Seeds + Anti-Scatter present. Under 200 lines.

## Path Rejection Declaration
- ❌ **A 15th manual pending-work list** — REJECTED (A8): re-forks the exact scatter this fixes; the spine is a GENERATED VIEW.
- ❌ **Copying Antigravity's Python modules (WorkspaceReconciler/CxpWatcher) wholesale** — REJECTED: native-primitives first (bash/hooks); adopt the IDEAS, adapt the form.
- ❌ **Antigravity's bypass-permission + global-write whitelist** — REJECTED (security): CISEM just SCOPED permissions to the project root (Edit(./**), b9e5a72); adopting a bypass would undo it. The prompt-gate stays.

## CORE SEEDS (immutable)
[[CORE-SEED | MUST: the work-spine is a GENERATED VIEW over the existing homes — NEVER a 15th manual list; each item keeps its own SSOT, the view references them (I10/A8) | WHY: a new list re-creates the scatter it exists to cure | VERIFY: the view is a script that reads other files, holds no authoritative state of its own | APPLIES_TO: D1/D2 permanently]]
[[CORE-SEED | MUST: Antigravity's mechanizations are ADAPTED to CISEM native-primitives (bash/hooks/existing checks), never copied as heavy modules, and its bypass-permission/global-write pattern is NEVER adopted | WHY: A12 (external, adapt-don't-copy) + the scoped-permission gate (b9e5a72) is a security win to preserve | VERIFY: no Python-module port; settings.json stays scoped; no global-write | APPLIES_TO: all deliverables]]
[[CORE-SEED | MUST: no deliverable is "done" without a RUNNING mechanism shown (I23/Principle 18C) — the harvest mechanization (D3) counts only when the hook EXTRACTS, not merely cues | WHY: the whole harmony gap is declared>wired; this plan must not add to it | VERIFY: each D cites its running mechanism + evidence | APPLIES_TO: D1–D5]]

## Ratification Scope (SPLIT — per GI-68 Finding 3; the Governor ratifies these SEPARATELY)
The five deliverables have wildly different readiness; ratifying them as one event is scope-bundling. Opus recommends:
- **D1 + D2 (work-spine view + `cisem-status` CLI)** — genuinely NEW, self-contained, core-first → **RATIFY + AUTHORIZE BUILD now.**
- **D3 (WEE build, L1–L4; L5 OFF)** — ARCH-00426 is ALREADY Governor-RATIFIED → **SEQUENCE the build; NO new ratification needed** (this plan only orders it after D1/D2).
- **D4 (active-process router)** — has NO plan and NO Opus Stage-1 of its own → **carve out; needs its OWN Stage-1 → GI-68 before any build** (do NOT ratify here).
- **D5 (BLOCK-mode graduation)** — ARCH-00270 v2 is PROPOSED/COMMENTS-RECEIVED with an internal contradiction → **ASSESS-ONLY** (already scoped peripheral; no build/ratify here).

## Opus Stage-1 Soundness Verdict (for [RATIFY-GATE])
**SOUND-WITH-CONDITIONS → conditions now FOLDED (v0.3).** The CORE (D1 generated view + D2 CLI) is architecturally sound: A8-correct (a generated view, never a new list — Core-Seeded), A12-correct (Antigravity ideas adapted, bypass-permission rejected), consolidates 419/426/406, and the CDS-derived falsification-test discipline is a real strengthening. The GI-68 dual-review (both lenses, VERBATIM in `CISEM-ARCH-00432-GI-68-dual-review.md`) confirmed my three Stage-1 conditions + found three more drafting defects; all ACCEPTED findings are folded (see `## GI-68 Dual Review`). Process note (Finding 7): this GI-68 ran BEFORE this written Stage-1 verdict, deliberately — it INFORMED the verdict; GI-68 did not substitute for Stage-1. **Not self-ratified (GI-63 / Principle 20C):** this verdict clears the plan for the Governor's informed ratification of the SPLIT scope above — it does NOT stamp it ratified.

## GI-68 Dual Review (Opus value-assessment; verbatim verdicts → `CISEM-ARCH-00432-GI-68-dual-review.md`)
Dispatched 2026-08-08 via `/cisem-plan-verify` (cisem-sonnet design lens + cisem-haiku evidence lens). Both verdicts kept VERBATIM in the companion record (Principle 19). Opus value-assessment per finding:
| # | Finding (lens) | Sev | Opus | Action folded |
|---|---|---|---|---|
| F1 | D2 test uses frozen "43/12" literal (S+H) | MAJOR | ✅ ACCEPT | D2 falsification test → live `view==filesystem` relationship |
| F2 | ARCH-00419 is unratified + view-script unbuilt = ghost seed (S) | BLOCKER | ✅ ACCEPT | D1 reframed as NEW construction informed-by (not built-upon); dependency line corrected; **verified `planning-marker-view.sh` absent** |
| F3 | D1–D5 bundled under one ratification (S) | MAJOR | ✅ ACCEPT | added `## Ratification Scope` split (D1+D2 / D3 sequence-only / D4 own-Stage-1 / D5 assess-only) |
| F4 | Alignment table missing D3/D4 rows (S) | MAJOR | ✅ ACCEPT | added WEE + router rows |
| F5 | "14 homes" is really 11 distinct; parked-register naming; IBD-0017 double-count (S+H) | MAJOR | ✅ ACCEPT | corrected to 11 distinct + real filename + IBD-0017 folded into IBD |
| F6 | DoD reads D1-only (S) | MINOR | ✅ ACCEPT | DoD note: per-deliverable finish lines live in Falsification Tests |
| F7 | GI-68 ran before Stage-1 verdict (S) | MINOR | ✅ ACCEPT | stated explicitly in the Stage-1 verdict above (informing, not substituting) |
| F8 | todo-tracker home is real (S) | — | ✅ CONFIRM | no change (confirmatory) |
| H-x | `wisdom_considered` cites a "3-Haiku gather" with no on-disk artifact (H) | MINOR | ✅ ACCEPT | provenance clarified below (prior-session conversational gather; not a persisted artifact) |
_Provenance clarification (H-x):_ the "3-Haiku operating-spine gather 2026-08-07" was a PRIOR-SESSION conversational gather (INHERITED, not a persisted artifact) — its conclusion (the ~11-home scatter) is now re-verified on disk this review, so the plan no longer relies on the unpersisted claim.
_DoD note (F6):_ per-deliverable finish lines are the Falsification Tests above; the DoD prose describes D1's (the core-first proof), not the whole plan's.

## Change log
- v0.1 — 2026-08-07 (Opus drafted at Governor direction, after the operating-spine synthesis + reading Antigravity's Harmonious-Flow plan): the work-spine view as core; seams mechanized by adapting Antigravity's proven ideas to native-primitives; consolidates ARCH-00419/426/406. Awaits Opus soundness → GI-68 → Governor ratification.
- v0.3 — 2026-08-08 (Opus Stage-1 verdict + GI-68 dual-review folded, fresh tab, on Governor "I approve what previous tab suggested" = approve the REVIEW PROCESS, not a self-ratify): dispatched `/cisem-plan-verify` (cisem-sonnet + cisem-haiku); both verdicts VERBATIM in `CISEM-ARCH-00432-GI-68-dual-review.md`. Folded all 9 accepted findings — D2 test → live relationship (F1); D1 reframed NEW-construction, ARCH-00419 ghost-seed corrected + verified (F2); SPLIT ratification scope added (F3); D3/D4 alignment rows (F4); home-count 14→11 distinct + parked-register filename + IBD-0017 folded (F5); DoD/provenance notes (F6/F7/H-x). Status → GI-68-DUAL-REVIEWED, AWAITING GOVERNOR RATIFICATION of the split scope. NOT self-ratified (GI-63).
- v0.2 — 2026-08-07 (enriched from CDS's 7-Tier Harmonious-Flow plan — the THIRD independent convergence on the same 5-system map, built by Sonnet 4.6; A12 — ideas adopted, code not copied): added a FALSIFICATION TEST per deliverable (verify-behavior baked in — CDS's strongest pattern) + a GAP→deliverable traceability. Cross-refs CDS Tier-4 (live-existence-check, not format-only) for the routed-destination-exists guard + Tier-6 (park↔vault bridge) as the vault reference. Convergence across CISEM/Antigravity/CDS validates the synthesis is a real structural map, not one model's opinion.
